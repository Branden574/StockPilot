/**
 * Drawer route for Suppliers — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/suppliers-tab.tsx) renders the
 * exact same component without duplicating screen code. This route's path
 * (/suppliers) and behavior are unchanged.
 */
export { default } from '@/screens/suppliers';
