/**
 * Optional bottom-tab route for Movements (Settings → Customize tab bar).
 * Named movements-tab so it cannot collide with the drawer's /movements —
 * see orders-tab.tsx for the collision rationale. Renders the identical
 * shared screen component.
 */
export { default } from '@/screens/movements';
