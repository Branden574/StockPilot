/**
 * Optional bottom-tab route for Reports (Settings → Customize tab bar).
 * Named reports-tab so it cannot collide with the drawer's /reports — see
 * orders-tab.tsx for the collision rationale. Renders the identical shared
 * screen component.
 */
export { default } from '@/screens/reports';
