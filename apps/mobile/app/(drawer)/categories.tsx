/**
 * Drawer route for Categories — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/categories-tab.tsx) renders the
 * exact same component without duplicating screen code. This route's path
 * (/categories) and behavior are unchanged.
 */
export { default } from '@/screens/categories';
