/**
 * Drawer route for Tags — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/tags-tab.tsx) renders the
 * exact same component without duplicating screen code. This route's path
 * (/tags) and behavior are unchanged.
 */
export { default } from '@/screens/tags';
