/**
 * Drawer route for Procedures — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/procedures-tab.tsx) renders the
 * exact same component without duplicating screen code. This route's path
 * (/procedures) and behavior are unchanged.
 */
export { default } from '@/screens/procedures';
