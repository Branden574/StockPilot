/**
 * Drawer route for Orders — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/orders-tab.tsx) renders the exact
 * same component without duplicating screen code. This route's path (/orders)
 * and behavior are unchanged.
 */
export { default } from '@/screens/orders';
