/**
 * Drawer route for Locations — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/locations-tab.tsx) renders the
 * exact same component without duplicating screen code. This route's path
 * (/locations) and behavior are unchanged.
 */
export { default } from '@/screens/locations';
