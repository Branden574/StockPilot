/**
 * Drawer route for Reports — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/reports-tab.tsx) renders the exact
 * same component without duplicating screen code. This route's path
 * (/reports) and behavior are unchanged.
 */
export { default } from '@/screens/reports';
