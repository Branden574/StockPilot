/**
 * Drawer route for Rentals — thin wrapper over the shared screen so the
 * optional bottom tab (app/(drawer)/(tabs)/rentals-tab.tsx) renders the
 * exact same component without duplicating screen code. This route's path
 * (/rentals) and behavior are unchanged.
 */
export { default } from '@/screens/rentals';
