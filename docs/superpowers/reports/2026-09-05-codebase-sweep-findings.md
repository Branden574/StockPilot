# StockPilot codebase sweep — findings register

Generated 2026-09-05 from a full-repository sweep of `main` @ 09bf2157.

**Method.** 18 read-only domain reviewers produced 162 raw findings against the
live code (not the docs); overlapping reports were merged to 144; every one was
then handed to an adversarial verifier whose default verdict was REFUTED and
which had to re-derive the failure from the code, its tests and its migrations.
14 P0/P1 survivors were re-derived a second time by an independent verifier.
**3 were refuted and dropped. 141 stand.**

| Verdict | Count |
|---|---|
| Confirmed | 121 |
| Upgraded in severity | 9 |
| Downgraded in severity | 11 |
| **Refuted (dropped)** | **3** |

| Severity | Standing | Fixed in this pass | Open |
|---|---|---|---|
| P0 | 2 | 2 | 0 |
| P1 | 22 | 12 | 10 |
| P2 | 66 | 1 | 65 |
| P3 | 51 | 0 | 51 |
| **Total** | **141** | **15** | **126** |

## Refuted — do not re-raise

| ID | Claim | Why it is not a bug |
|---|---|---|
| SP-134 | purchase-orders.update.test.ts waits for fire-and-forget notifications with real 50 ms setTimeout sleeps — tim | The claimed mechanism ('under load the un-awaited chain takes >50 ms and the assertion sees 0 or 1 calls') cannot happen in this test. The block under test is purchase-orders.ts:964-993 `void (async () => { const admin = createAdminClient(); const { data: members } = await admin.from(...).select(...).eq(...).in(...).no |
| SP-118 | services/push.ts notifyUser() is dead code that re-enables the documented double-push regression if anyone imp | The premise 'zero importers / dead code' is false. apps/web/src/app/api/v1/push/test/route.ts:4 `import { notifyUser } from '@/server/services/push';` and :33 `const delivered = await notifyUser({ userId: ctx.userId, ... })` — the authenticated self-service push test, and the mobile app calls it: apps/mobile/app/(drawe |
| SP-111 | cycle-count record route reports service validation_error as HTTP 409 (state conflict) instead of 400 | The claimed failure cannot occur: CycleCountsService.recordCount never throws 'validation_error'. Its full throw surface (apps/web/src/server/services/cycle-counts.ts:1110-1160): `assertModuleEnabled` (:1118 -> module_disabled), `assertPermission(this.ctx,'stock:adjust')` (:1119 -> forbidden), `assertSessionAccess` (:1 |

## Register

Status: **FIXED** = shipped on `fix/sweep-pass-b-security`. **OPEN** = verified, not yet fixed.

| ID | Sev | Status | Area | Problem | File | Fix (verified plan) |
|---|---|---|---|---|---|---|
| SP-001 | P0 | **FIXED** — 0346 + pgTAP 0346 (27) | migrations-db-security | apply_cycle_count_location_delta is an ungated SECURITY DEFINER RPC executable by any authenticated user: cross-org stock inflation/drain with no ledger row (CONFIRMED) | `supabase/migrations/0342_cycle_count_location_reconciliation.sql:160` | NEW migration supabase/migrations/0346_gate_cycle_count_location_delta.sql (DB layer, core): `create or replace function public.apply_cycle_count_location_delta(uuid,uuid,uuid,numeric)` re-stating the 0342 body verbatim with a gate prepended BEFORE the early returns: derive `v_org` from `public.inve |
| SP-002 | P0 | **FIXED** — 0347 + pgTAP 0347 (19) + web/mobile tests | mobile-offline-sync | Bundle distribute is queued on ANY error and replayed with no server idempotency -> duplicate distributions (double stock draw) | `apps/mobile/app/bundles/[id].tsx:171` | Migration 0346: `drop function public.distribute_bundle(uuid,numeric,uuid,boolean,uuid,text)` and recreate with `p_idempotency_key text default null`; when non-null, look up idempotency_keys(organization_id=v_org, scope='bundle_distribution', key) — completed -> return resource_id; mismatched reques |
| SP-003 | P1 | **FIXED** — cycle-count-cache assigned_to + wiring test | cycle-counts | Mobile assignee lock never engages: cache drops assigned_to, so a non-assignee gets an editable count and the assignee never sees Release | `apps/mobile/src/lib/cycle-count-cache.ts:86` | Mobile cache layer only (apps/mobile/src/lib/cycle-count-cache.ts): add `assignedTo?: string \| null` to cacheCycleCount's header param, add `assigned_to` to the insert column list binding `header.assignedTo ?? null`, select `assigned_to` in getCycleCount and listCachedCycleCounts and map it to `ass |
| SP-004 | P1 | **FIXED** — document-threat-scan.ts + 3 tests | ai-uploads-files | PDF threat scanner reports CLEAN once the total inflate budget is exactly exhausted — /JavaScript after 8 x 32 MiB filler streams passes (proven) | `apps/web/src/lib/document-threat-scan.ts:337` | lib layer only (apps/web/src/lib/document-threat-scan.ts scanFlateStreams): before calling inflateSync, `if (budget <= 0) return { code: 'pdf_scan_limit', detail: 'total inflate budget exhausted' };` and in the catch map `ERR_OUT_OF_RANGE` (any non-Z_DATA_ERROR zlib error, i.e. anything except code  |
| SP-005 | P1 | OPEN | web-mobile-parity | Mobile quick-adjust (scan tab + item screen) calls adjust_stock RPC directly, bypassing every web-side stock:adjust rule | `apps/mobile/app/(drawer)/(tabs)/scan.tsx:439` | API route layer + mobile screens (OTA), no migration. Add apps/web/src/app/api/v1/items/[id]/adjust/route.ts mirroring remove-stock/route.ts: withApiContext, checkRateLimit(`stock-adjust:${userId}`, 60, 60_000), fail-fast `assertPermission(ctx, 'stock:adjust')`, zod body { quantityChange: z.coerce.n |
| SP-006 | P1 | **FIXED** — drain-failure terminal refusals + tests | cycle-counts | A record_count outbox row refused because the count was posted/cancelled/reassigned elsewhere retries forever and blocks mobile posting of EVERY count on the device | `apps/mobile/src/lib/cycle-count-sync.ts:246` | Mobile sync engine (apps/mobile/src/lib/drain-failure.ts + cycle-count-sync.ts): add a record_count-specific verdict -- a 404 (not_found), 403 (forbidden) or 409 (conflict) from the record endpoint is terminal -> outboxReject(row.id, msg) (which already clears local_dirty and lands the row in Settin |
| SP-007 | P1 | OPEN | purchasing-receiving | Mobile PO receive calls post_receipt_v2 directly with p_request_hash = the idempotency key, so a retried edit silently returns the OLD receipt as success — and every Rece | `apps/mobile/app/po/[id].tsx:468` | API + mobile layers (no DB change). Add POST apps/web/src/app/api/v1/po/[id]/receipts/route.ts — Bearer twin of the web receive action: validate with postReceiptSchema from @stockpilot/core (lines by poLineId), call `new ReceivingService(ctx).postReceipt({...})` (computes the real hash, maps idempot |
| SP-008 | P1 | OPEN | auth-sessions | Account deletion reports success on deleteUser failure, but the 'tombstone blocks login' premise is false — no auth funnel reads user_profiles.deleted_at | `apps/web/src/server/actions/profile.ts:452` | Action + API route layer (no DB change). In profile.ts deleteOwnAccountAction and api/v1/account/delete/route.ts: when `authErr` is set, (1) best-effort `admin.rpc('admin_revoke_user_sessions', { p_user_id })` (0308) so the requester is at least signed out everywhere, (2) leave `deleted_at` stamped  |
| SP-009 | P1 | **FIXED** — deleted_at filter + error logged | supabase-query-hygiene | Item cost history report filters on a column that does not exist (`is_deleted`) — picker is always empty, error unchecked | `apps/web/src/app/(dashboard)/dashboard/reports/item-cost-history/page.tsx:41` | Page layer only: replace `.eq('is_deleted', false)` with `.is('deleted_at', null)`; destructure `error` and log + render a fail-closed inline notice (pattern #1) instead of silently mapping `[]`. Preferably source the picker from `InventoryService.listNames()` (already the app-wide dropdown loader)  |
| SP-010 | P1 | **FIXED** — reset effect keyed on open + 2 tests | react-web-correctness | Receive-items dialog wipes in-progress receipt entry on any RSC refresh (realtime nudge) because its reset effect depends on the fresh-per-render lines prop | `apps/web/src/components/po/po-receive-dialog.tsx:130` | Component layer only (po-receive-dialog.tsx:119-130): change deps to `[open]` with `// eslint-disable-next-line react-hooks/exhaustive-deps -- reset ONLY on open transition; `lines` is a fresh array on every RSC refresh (realtime nudge) and must not wipe in-progress entry` — matching remove-from-rac |
| SP-011 | P1 | **FIXED** — cycle-count-snapshot-sql + real-SQLite test | mobile-offline-sync | Every 60s snapshot pull REPLACES cycle_counts/cycle_count_lines and destroys the offline cycle-count cache (cached_at -> NULL, local_dirty wiped, item names wiped) | `apps/mobile/src/lib/sync.ts:198` | Service layer, sync.ts only. Header: `insert into cycle_counts (id,status,warehouse_id,started_at,assigned_to,notes,last_synced_at) values (…) on conflict(id) do update set status=excluded.status, warehouse_id=excluded.warehouse_id, assigned_to=excluded.assigned_to, notes=excluded.notes, last_synced |
| SP-013 | P1 | OPEN | purchasing-receiving | PO-import approve() has no atomic status claim — two overlapping approves create two purchase orders from one import | `apps/web/src/server/services/po-imports.ts:1353` | Service layer (po-imports.ts approve). Claim FIRST: `const { data: claimed } = await supabase.from('po_imports').update({ status:'approved', approved_by: userId, approved_at: now }).eq('organization_id', orgId).eq('id', id).in('status', ['parsed','needs_review']).select('id').maybeSingle(); if (!cla |
| SP-014 | P1 | OPEN | auth-sessions | Password reset via email link cannot complete for any TOTP-enrolled user (GoTrue refuses updateUser at AAL1) | `apps/web/src/server/actions/auth.ts:414` | Core + action + page layer. (1) packages/core completePasswordResetSchema: add optional `totpCode` (6 digits) / `recoveryCode`. (2) auth.ts completePasswordResetAction: after getUser, `listFactors()`; if a verified TOTP factor exists and `getAuthenticatorAssuranceLevel().currentLevel !== 'aal2'`: if |
| SP-015 | P1 | **FIXED** — session.ts membership-derived org + removeMember cleanup + test | auth-sessions | requireOrgContext builds a context for an org the user was REMOVED from, with the role of a different org (stale default_organization_id) | `apps/web/src/lib/auth/session.ts:235` | Core (session.ts:235): `const targetOrgId = orgId ?? defaultOrgId;` — derive from the loader's resolved membership; treat session.defaultOrganizationId purely as the preference the loader already honours at :178-183. Service (team.ts removeMember, and any self-leave path if one is added): after the  |
| SP-016 | P1 | **FIXED** — send result checked + 5 tests | async-concurrency-server | Team invite and resend-invite report success and audit 'user.invited' when Resend returned ok:false — the invite email is silently never sent | `apps/web/src/server/services/team.ts:299` | Service layer only (apps/web/src/server/services/team.ts). In invite(): `const sent = await sendEmail(...); if (!sent.ok) { void reportError(new Error(sent.error), { tag: 'team.invite.email', organizationId: this.ctx.organizationId }); throw new ServiceError('internal_error', 'The invite was created |
| SP-017 | P1 | **FIXED** — api.orgHeader() + class-guard test | mobile-offline-sync | Raw `fetch` callers omit X-Organization-Id, so a multi-org user's AI-scan counts and PO scans resolve to their DEFAULT org, not the active workspace | `apps/mobile/app/cycle-count/ai-scan/[id].tsx:289` | Component/lib layer only: export `async function orgHeader(): Promise<Record<string,string>>` from apps/mobile/src/lib/api.ts (same AsyncStorage read api() already does at :115) and spread it into the six raw call sites (ai-scan upload, per-line record, confirm; po-imports scan; ai chat POST; extrac |
| SP-018 | P1 | OPEN | ai-uploads-files | Mobile PO/order attachments write the metadata row directly via PostgREST after a direct storage PUT — the finalize-time sniff + threat scan never runs on that path | `apps/mobile/src/components/po-attachments.tsx:177` | Sequenced: (1) api route layer — add Bearer twins POST /api/v1/purchase-orders/[id]/attachments and POST /api/v1/orders/[id]/attachments that mint nothing new and simply call PoAttachmentsService.add / OrderAttachmentsService.add on the client-uploaded path (finalize pattern used by maintenance-requ |
| SP-019 | P1 | OPEN | time-money-pagination-forms | PO expected_at is stored as UTC midnight but rendered in Pacific: supplier-facing PO PDF, PO list (instant mode), mobile ETA and the overdue count all show/act one day ea | `apps/web/src/lib/pdf/po.tsx:462` | Treat expected_at as a calendar date everywhere (no migration for the minimal fix). (1) lib/pdf/po.tsx:462 -> `formatDateForPdf(po.expectedAt, 'UTC')`. (2) lib/utils.ts: add `formatDateOnly(iso)` = Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',timeZone:'UTC'}) and use it in po-instant-tab |
| SP-021 | P1 | **FIXED** — outbox supersede + latestRowsPerLine tests | cycle-counts | Offline outbox can replay an OLDER count over a newer one for the same line (lost offline work) | `apps/mobile/src/lib/cycle-count-cache.ts:327` | Mobile cache layer (cycle-count-cache.ts updateLocalLine): inside the existing transaction, delete (or mark 'superseded') pending/failed record_count rows whose payload lineId matches before inserting the new row -- one live row per line; a row in 'sending' must NOT be deleted (in flight), so also a |
| SP-022 | P1 | OPEN | auth-sessions | 'Enrolled TOTP forces AAL2' is app-layer only: an AAL1 token reads/writes every org table via direct PostgREST (no RLS policy or membership helper checks the aal claim) | `apps/web/src/lib/auth/api-context.ts:286` | DB migration (new file — note supabase/migrations/0346_gate_secdef_stock_helpers.sql already exists uncommitted, so use 0347+) with pgTAP: add ONE zero-arg STABLE SECURITY DEFINER helper `session_mfa_satisfied()` returning `coalesce(current_setting('request.jwt.claims', true)::jsonb->>'aal','aal1')  |
| SP-023 | P1 | OPEN | supabase-query-hygiene | Rental markReturned/cancel: status UPDATE is fail-open (no org filter, no row proof) while reservations are released via service role — RLS floor (warehouse WRITE) is str | `apps/web/src/server/services/rentals.ts:270` | Service layer (rentals.ts): (1) select `warehouse_id` in the pre-read and `assertWarehouseAccess(row.warehouse_id, 'write', this.ctx)` so the gate matches the RLS floor (pattern #4); (2) append `.eq('organization_id', this.ctx.organizationId).select('id').maybeSingle()` to both status UPDATEs and th |
| SP-037 | P1 | **FIXED** — drain-failure + scoped Post gate | mobile-offline-sync | drainQueue retries permanently-refused (4xx) rows every 60s forever with no backoff and no UI; the discarded result also blocks posting every cycle count | `apps/mobile/src/lib/sync.ts:324` | Service/component layers. (a) drain-failure.ts: treat 400/403/404/422 as terminal 'rejected' (surfaced in Unsent work with last_error), keep 409/429/5xx/network retryable; both drains already share this classifier so the fix lands in one place. (b) sync.ts drainQueue: read via a backoff-filtered `li |
| SP-077 | P1 | **FIXED** — receipt-request-hash + tests | mobile-offline-sync | PO receive sends the idempotency key AS the request hash, so a retry after a lost ack with edited lines silently returns the first receipt | `apps/mobile/app/po/[id].tsx:453` | Component + a tiny lib helper (OTA-shippable, no native module): add apps/mobile/src/lib/receipt-request-hash.ts exporting buildReceiptRequestHash(input) that returns the canonical JSON string of {purchaseOrderId, warehouseId, notes, lines sorted by po_line_id with qty fields .toFixed(4)} - mirror h |
| SP-090 | P1 | **FIXED** — same defect as SP-002 — closed by 0347 | async-concurrency-server | Mobile outbox 'distribute_bundle' replays with no idempotency key, and neither the /api/v1 route nor distribute_bundle RPC dedupes — a lost 2xx double-draws stock (latent | `apps/mobile/src/lib/sync.ts:370` | DB (new migration — note 0346_gate_secdef_stock_helpers.sql already exists untracked, so use 0347): `alter table bundle_distributions add column idempotency_key uuid;` + `create unique index bundle_distributions_org_idem_uniq on bundle_distributions(organization_id, idempotency_key) where idempotenc |
| SP-012 | P2 | OPEN | dead-code-duplication | Mobile rental checkout re-implements the web rule divergently: bare rentals header, no lines, no stock reservations, while its comment claims it is the same path as web | `apps/mobile/app/rentals/new.tsx:89` | API route + mobile component, no DB change. Add apps/web/src/app/api/v1/rentals/route.ts (withApiContext Bearer twin, same shape as api/v1/orders) parsing createRentalSchema and calling RentalsService.create (which already enforces module, rentals:create, is_rental/warehouse validation, reservations |
| SP-020 | P2 | OPEN | orders-reservations | Digital signature path never emails internal (member) requesters; backorder emails never reach them on either path | `apps/web/src/app/api/orders/sign/route.ts:328` | Service/route layer, no migration. Extract resolveRecipient (order-requests.ts:3397) into a shared helper `resolveOrderRequesterContact(client, {requester_email, requester_user_id, requester_name})` under apps/web/src/server/lib/ (accepting either the user client or the admin client). In sign/route. |
| SP-024 | P2 | OPEN | supabase-query-hygiene | PO-import approve never checks the `po_imports → approved` UPDATE — a failed stamp leaves a live PO behind an import that can be approved again (duplicate PO) | `apps/web/src/server/services/po-imports.ts:1719` | Service layer, two steps: (a) minimal — destructure `{ data: stamped, error }`, append `.eq('organization_id', this.ctx.organizationId).select('id').maybeSingle()`, throw ServiceError('internal_error') on error or null row before audit/return (pattern #2); (b) preferred — move the status transition  |
| SP-025 | P2 | OPEN | orders-reservations | OrderRequestsService.get().pickSlipStale is always false — line select omits created_at (pattern #5) | `apps/web/src/server/services/order-requests.ts:839` | Service layer only: add `created_at` to the lines select at order-requests.ts:710 and carry `created_at: (r.created_at as string \| null) ?? null` onto each flatLine (extend OrderRequestLineWithItem with an optional created_at), or compute staleness from the raw `lines` rows before mapping. Keep the |
| SP-026 | P2 | OPEN | api-routes | Mobile self-deletion audit row is silently dropped: audit() called without ctx in a Bearer route | `apps/web/src/app/api/v1/account/delete/route.ts:117` | Route layer only: apps/web/src/app/api/v1/account/delete/route.ts:117 -> `await audit({ ...same payload }, ctx);` (mirror v1/movements/[id]/note). Optional hardening in server/services/audit.ts: when `ctx` is absent and the caught error is a NEXT_REDIRECT, report at level 'error' with tag 'audit.mis |
| SP-027 | P2 | OPEN | api-routes | Raw Postgres/PostgREST/provider error text echoed in API error bodies across ~20 routes (S13 posture violation) | `apps/web/src/app/api/v1/size-counts/route.ts:82` | Route layer, smallest change per site: replace the raw `message: <err>.message` / `String(e)` with a stable slug plus `void reportError(e, { tag })` (the majority pattern, e.g. inventory/export.csv:126-127). Sites: v1/account/delete:112 and :138; orders/freq:53; ai/sessions/route.ts:24,39; ai/sessio |
| SP-028 | P2 | OPEN | migrations-db-security | security_invariants sweeps only anon-executable SECURITY DEFINER functions; nothing sweeps authenticated-executable ones, which is how the 0342 helper shipped green | `supabase/tests/security_invariants.test.sql:175` | Test layer only (supabase/tests/security_invariants.test.sql + docs/security/SECURITY-INVARIANTS.md section 2), shipped in the same PR as migration 0346 so the sweep is green on day one; the sweep must NOT be made green by allowlisting the four ungated functions. Also add `_cycle_count_location_fact |
| SP-030 | P2 | OPEN | dead-code-duplication | Unreferenced scripts/dev-local.sh and dev-prod.sh write THROUGH the .env.local symlink and rewrite the canonical env file outside the repo, dropping every key they do not | `scripts/dev-local.sh:63` | Delete scripts/dev-local.sh and scripts/dev-prod.sh (unreferenced) and update the pointer in memory project_env_files_move_outside_repo.md:29 / project_code_cleanup_followups.md:14. If a local-stack switcher is still wanted, rewrite it to refuse when `-L "$WEB_ENV"` and to write a separate non-symli |
| SP-031 | P2 | OPEN | web-mobile-parity | Four live notification link shapes have no mobile rewrite and dead-end on Home although native twins exist | `apps/mobile/src/lib/web-path-rewrite.ts:47` | Mobile lib only (OTA). In web-path-rewrite.ts insert before the catch-all: `{ re: new RegExp(`/dashboard/cycle-counts/${UUID}`), to: (m) => `/cycle-count/${m[1]}` }`, `{ re: new RegExp(`/dashboard/bundles/${UUID}`), to: (m) => `/bundles/${m[1]}` }`, `{ re: /\/dashboard\/purchase-orders(\?.*)?$/, to: |
| SP-032 | P2 | OPEN | cycle-counts | Mobile detail loads cycle_count_lines with an unbounded select — silently capped at 1000 rows, so a large count is only partially countable on mobile | `apps/mobile/app/cycle-count/[id].tsx:181` | Mobile screen/cache: replace the bare select with the paged helper above (mirror apps/web/src/server/services/lib/paginate.ts, stable `.order('id')`, 1000-row `.range()` windows); apply the same to any other mobile read of cycle_count_lines (scan/ai-scan read from the cache, so the detail fetch is t |
| SP-033 | P2 | OPEN | orders-reservations | orders:approve is declared FULLY_GRANTABLE but every order RPC still gates on has_org_role('manager') — granted staff can deny but not approve (pattern #4) | `packages/core/src/constants/permissions.ts:351` | Migration 0347+ (DB layer): re-issue `create or replace` for approve_order_request, approve_partial, resume_fulfillment, close_partial, reopen_picking, assign_picking and cancel_order_request with the gate `if not (public.has_org_role(v_req.organization_id,'manager') or public.has_permission(v_req.o |
| SP-034 | P2 | OPEN | react-web-correctness | Movements instant-mode search cannot be cleared by backspacing: the debounced q effect early-returns when q equals the constant initial.q, so onChange never fires with th | `apps/web/src/components/movements/movements-filter-bar.tsx:78` | Component layer (movements-filter-bar.tsx:76-83): replace the `q === initial.q` mount guard with a last-APPLIED ref: `const appliedQ = React.useRef(initial.q);` effect: `if (q === appliedQ.current) return; const t = setTimeout(() => { appliedQ.current = q; apply({ q, ...latest.current }); }, 300);`  |
| SP-035 | P2 | OPEN | email-notifications | Schedule reminders and rental emails print times and the today/tomorrow word in hard-coded America/Los_Angeles although organizations.timezone is a configurable setting e | `apps/web/src/app/api/cron/schedule-reminders/route.ts:52` | Cron/lib layer, no migration. schedule-reminders/route.ts: add `organizations.timezone` via one batched select on the distinct organization_ids in `data` (Map<orgId, tz>), replace DISPLAY_TZ with a per-event `const tz = resolveOrgTimezone(tzByOrg.get(ev.organization_id))` passed into tzPart/tzDateKe |
| SP-036 | P2 | OPEN | mobile-offline-sync | AI-scan confirm double-writes each line: the outbox replay lacks aiScanId and NULLs the ai_scan_id attribution the direct write just set; doubles request volume into the  | `apps/mobile/app/cycle-count/ai-scan/[id].tsx:310` | Route: pass `aiScanId: parsed.data.aiScanId` (leave undefined undefined) so the service's documented preserve-on-manual semantics actually apply. Mobile lib: add `mirrorLocalLine(lineId, counted)` in cycle-count-cache.ts that updates `counted` only (no dirty flag, no outbox row) and use it at ai-sca |
| SP-038 | P2 | OPEN | tests-quality-gaps | verifyStoredDocumentOrDelete's whole-object re-download and 'unreadable now → fail closed' branches are never exercised — every test stubs totalSize == prefix length | `apps/web/src/server/services/attachment-byte-guard.test.ts:66` | Test layer only (apps/web/src/server/services/attachment-byte-guard.test.ts). No source change. Confirm in the first run that scanDocumentBytes flags a bare, non-Flate `/Launch` in the catalog (document-threat-scan.ts:154 suggests it does); if it only scans inside streams, wrap the payload in a plai |
| SP-039 | P2 | OPEN | tests-quality-gaps | completePasswordResetAction has zero tests — its global sign-out + live-session revoke are security properties documented only in comments | `apps/web/src/server/actions/auth.ts:424` | Test layer only: add the cases to apps/web/src/server/actions/auth.password-reset.test.ts (already in the security-test.sh WEB_TESTS manifest, so the new pins join the named gate automatically). No source change. |
| SP-040 | P2 | OPEN | ai-uploads-files | AI aggregation tools request 50,000 / 10,000 rows but PostgREST and MovementsService clamp to 1,000 — totals, top movers and 'dead stock' are silently wrong above 1k rows | `apps/web/src/lib/ai/tools.ts:526` | lib/service layer, no migration: replace the three inventory_items selects in tools.ts with fetchAllRows(buildPage => query.order('id').range(from,to), { cap: 20_000 }) and return `truncated: rows.length >= cap`; for the three movement tools add a column-only paged reader to MovementsService (e.g. l |
| SP-041 | P2 | OPEN | dead-code-duplication | Lot trace passes an unescaped needle to ilike twice while four private copies of escapeIlike exist in sibling services (pattern #16 via #26) | `apps/web/src/server/services/lots.ts:267` | Service layer, no migration. Extract apps/web/src/server/services/lib/escape-ilike.ts exporting escapeIlike (the existing 3-char body); import it in lots.ts at :267 and :281, po-imports-lines.ts:252, and api/search/route.ts (escape after the existing strip so `_` is literal); replace the four privat |
| SP-042 | P2 | OPEN | time-money-pagination-forms | Audit log 'Until' date filter excludes the whole selected day: date-only param is passed straight to .lte(created_at) | `apps/web/src/server/services/audit-log.ts:134` | Service layer, apps/web/src/server/services/audit-log.ts list(): if `filters.until` matches /^\d{4}-\d{2}-\d{2}$/ convert with parseToDateParam (lib/movements-filters.ts is pure, client-safe, already the shared helper) and use `.lt`; normalise a date-only `since` with parseFromDateParam. Keep `.lte` |
| SP-043 | P2 | OPEN | time-money-pagination-forms | Auto-created schedule event 'Needed by ...' details string is formatted in the server's (UTC) local time, then copied into every reminder push/email body | `apps/web/src/server/services/order-requests.ts:1975` | Service layer, order-requests.ts autoScheduleFromOrder: `const tz = await getCachedOrgTimezone(row.organization_id); details: `Auto-created from order ${so}. Needed by ${formatOrgDateTime(row.needed_by, { dateStyle: 'medium', timeStyle: 'short' }, tz)}.`` (helpers already exist: lib/dashboard/cached |
| SP-044 | P2 | OPEN | time-money-pagination-forms | Recurring PO monthly/quarterly cadence drifts and skips months: setUTCMonth on day 29-31 rolls over, and runDueTemplates chains the roll | `packages/core/src/purchasing/recurring.ts:14` | Core, packages/core/src/purchasing/recurring.ts: replace setUTCMonth with an `addUtcMonthsClamped(d, n)` helper - compute target year/month, `day = Math.min(d.getUTCDate(), new Date(Date.UTC(y, m + 1, 0)).getUTCDate())`, then `new Date(Date.UTC(y, m, day, hh, mm, ss, ms))`. Use it for monthly (+1) a |
| SP-045 | P2 | OPEN | auth-sessions | changePasswordAction revokes other sessions' refresh tokens but never broadcasts, so other devices keep working for up to an hour after a password change | `apps/web/src/server/actions/auth.ts:559` | Action layer only (auth.ts changePasswordAction, after the signOut block): `const { data: { session } } = await supabase.auth.getSession(); const keepId = session?.access_token ? sessionIdFromJwt(session.access_token) : null; if (keepId) { await broadcastToChannel(`user:${user.id}:sessions`, 'revoke |
| SP-046 | P2 | OPEN | async-concurrency-server | Order status emails swallow a failed Resend send twice over — no reportError, so an outage in order email delivery is invisible (pattern #28 'a swallowed error is unmonit | `apps/web/src/lib/email/order-requests.ts:1236` | Service layer + api route. (a) lib/email/order-requests.ts: `return await sendEmail(...)` and change the signature to `Promise<SendResult>`. (b) services/order-requests.ts notifyEmail: `const sent = await sendOrderRequestEmail(...); if (!sent.ok) void reportSrvError(new Error(sent.error ?? 'send fai |
| SP-047 | P2 | OPEN | orders-reservations | suggestNeededByAction hard-codes a -07:00 / America/Los_Angeles offset instead of the org timezone | `apps/web/src/server/actions/order-requests.ts:204` | Action layer + a small core helper, no migration. In suggestNeededByAction read `organizations.timezone` (or getCachedOrg) and `const zone = resolveOrgTimezone(tz)`; change the prompt to ask for a zone-less wall-clock 'YYYY-MM-DDTHH:mm' interpreted in `${zone}` (and tell it 'now' in that zone for re |
| SP-048 | P2 | OPEN | react-web-correctness | PO size-run add dialog resets typed quantities whenever the page re-renders (same class as the receive dialog): [open, runs] where runs is memoized on the server-supplied | `apps/web/src/components/po/po-form.tsx:507` | Component layer (po-form.tsx:503-509): reset on `[open]` only with `// eslint-disable-next-line react-hooks/exhaustive-deps -- reset ONLY on open; runs is re-derived from server props on every RSC refresh`, or key the effect on a stable `runs.map(r=>r.groupId).join('\|')` string so only a genuinely  |
| SP-049 | P2 | OPEN | react-web-correctness | InventoryRealtime tears down and re-subscribes its Realtime channel on every render because the tables default-parameter array is a fresh identity in the effect deps | `apps/web/src/components/realtime/inventory-realtime.tsx:172` | Component layer (inventory-realtime.tsx): hoist `const DEFAULT_TABLES = [...] as const` to module scope and default `tables = DEFAULT_TABLES`; key the effect on a memoised `tablesKey = tables.join(',')` (and rebuild the list from it inside the effect) so inline-array callers are stable too; leave cl |
| SP-050 | P2 | OPEN | tests-quality-gaps | OrderRequestsService.cancel — the M7 requester self-cancel guard and error mapping have no test at service or action layer | `apps/web/src/server/services/order-requests.ts:1827` | Test layer only: add order-requests.cancel.test.ts. Optional hardening (not required by this finding): a pgTAP assertion in a new 0347 test that a requester at 'approved' is refused would need the RPC to carry the rule; today it deliberately does not, so leave the DB alone unless the owner wants the |
| SP-051 | P2 | OPEN | tests-quality-gaps | signInAction's MFA step-up decision, closed-mode rate limits, and challengeFactorAction/verifyEnrollmentAction have no behavioural tests — the only signInAction 'test' gr | `apps/web/src/server/actions/auth.account-disabled.test.ts:42` | Test layer only: add auth.sign-in.test.ts and mfa.test.ts, register both in scripts/security-test.sh WEB_TESTS (Identity, MFA, sessions block), and replace the source-grep describe in auth.account-disabled.test.ts with the behavioural case (e). No source change to auth.ts or mfa.ts. |
| SP-052 | P2 | OPEN | dead-code-duplication | Rental checkout has no reservation-aware availability guard on any layer: server TODO trusts the picker, and the picker cap uses quantityOnHand instead of the availabilit | `apps/web/src/server/services/rentals.ts:162` | Service layer (the check that matters): in RentalsService.create after the is_rental/warehouse loop (:151-160), read active reservations with the same query rentals/new/page.tsx:117-122 already uses (`from('stock_reservations').select('item_id, quantity').eq('organization_id', org).in('item_id', ite |
| SP-054 | P2 | OPEN | web-mobile-parity | Mobile admin role change writes organization_members directly: fail-open 0-row update, no audit row, no security event | `apps/mobile/app/(drawer)/admin/users.tsx:197` | API route + mobile screen (OTA), no migration. Add POST /api/v1/team/members/[id]/role: withApiContext, rate limit, zod { role: z.enum(['admin','manager','staff','viewer', ...ASSIGNABLE]) }, `new TeamService(ctx).updateMemberRole(id, role)`, serviceErrorStatus mapping. Mobile users.tsx: add `id` to  |
| SP-055 | P2 | OPEN | web-mobile-parity | Mobile cycle-count post calls post_cycle_count RPC directly, skipping the module gate, cycle_count.posted audit and cycle_count.completed webhook | `apps/mobile/app/cycle-count/[id].tsx:373` | API route + mobile screen (OTA), no migration. Add apps/web/src/app/api/v1/cycle-counts/[id]/post/route.ts: withApiContext, uuid check, checkRateLimit(`cycle-count-post:${userId}`, 30, 60_000), `await new CycleCountsService(ctx).post(id)`, return { ok: true, cycleCount }, map ServiceError via servic |
| SP-056 | P2 | OPEN | purchasing-receiving | stagedWorklist's item_stock_levels query is unpaginated — staged/unplaced holdings past PostgREST's 1000-row cap silently vanish from the put-away worklist (web + mobile) | `apps/web/src/server/services/inventory.ts:6720` | Service layer only: replace `const { data: levels, error } = await q` with `const levels = await fetchAllRows<Record<string, any>>((from, to) => q.order('id').range(from, to))` inside a try/catch that keeps the current fail-closed `return []` (fetchAllRows throws ServiceError on a page error). Separ |
| SP-057 | P2 | OPEN | inventory-accounting | Movements page blanks the amount of every order pick and reopen reversal (transfer rows with a real delta and null moved_quantity) | `apps/web/src/components/movements/movements-instant-table.tsx:174` | Component/core layer, no migration. Export ONE amount rule from @stockpilot/core (movement-history.ts already has it: delta when quantityChange !== 0, else moved when movedQuantity != null, else null) and consume it from all three copies: movements-instant-table.tsx:156-181, dashboard/movements/page |
| SP-058 | P2 | OPEN | auth-sessions | verifyPasswordSideChannel creates a real GoTrue session per check and never signs it out — ghost 'Unknown device' rows in Active Sessions and live refresh tokens in auth. | `apps/web/src/lib/auth/verify-password.ts:41` | Core helper only (apps/web/src/lib/auth/verify-password.ts): after a successful grant, `try { await client.auth.signOut({ scope: 'local' }); } catch { /* best-effort: the password IS verified; a leaked ghost is the pre-fix status quo */ }`. supabase-js keeps the session in a memory storage adapter w |
| SP-059 | P2 | OPEN | migrations-db-security | ensure_org_placement_locations / ensure_warehouse_placement_locations let any authenticated user inject Staging/Unplaced location rows into any org or warehouse (CONFIRME | `supabase/migrations/0194_apply_level_delta.sql:20` | In the same NEW migration 0346 (DB layer): re-create both helpers verbatim with the 0331/0341 gate prepended — ensure_org_placement_locations: `if auth.uid() is not null and not public.is_org_member(p_org) then raise exception 'forbidden' using errcode='42501'; end if;` after the null check; ensure_ |
| SP-060 | P2 | OPEN | tests-quality-gaps | Mobile offline drain loops (sync.ts drainQueue, CycleCountSyncEngine.drainOutbox) are covered only by source-text string pins; no test executes the mark-sending → send →  | `apps/mobile/src/lib/sync.ts:301` | Mobile test layer only: add the two behavioural test files above using vi.mock (the warehouse-scope.test.ts precedent proves the modules load once './db' and expo-network are mocked). Optional but not required: the proposed pure `drainPendingActions(deps)` extraction - defer it; a mocked test reache |
| SP-061 | P2 | OPEN | web-mobile-parity | Offline outbox retries permanently-rejected 4xx rows forever with no discard path; sync badge sticks on 'failing' | `apps/mobile/src/lib/drain-failure.ts:43` | Mobile lib only. drain-failure.ts: classify numeric status 400/404/409 as 'rejected' regardless of accountDisabled (401 rule unchanged; 403, 429, 5xx, network stay 'failed'). Both engines already route 'rejected' to markRejected/outboxReject (which clears the line's local_dirty in one transaction) a |
| SP-062 | P2 | OPEN | cycle-counts | assertSessionAccess on a null-warehouse count reads every line's warehouse_id unbounded — gate silently evaluates a 1000-row subset and runs on every keystroke | `apps/web/src/server/services/cycle-counts.ts:1044` | Service layer (apps/web/src/server/services/cycle-counts.ts assertSessionAccess): wrap the line scan in fetchAllRows with `.order('id').range(from, to)` -- smallest safe change, no migration. Better follow-up (new migration 0346+): a SECURITY INVOKER SQL function `cycle_count_line_warehouses(p_cycle |
| SP-063 | P2 | OPEN | purchasing-receiving | post_receipt_v2 accepts DRAFT purchase orders, so receiving bypasses the PO approval threshold and flips an unapproved draft straight to received | `supabase/migrations/0296_post_receipt_v2_serial_optional.sql:91` | NEW migration 0347+ (0346 is already taken in the working tree): seventh rewrite of post_receipt_v2 — dump the live body with pg_get_functiondef() per the 0296 provenance discipline, change only the status set to ('expected_inbound','ordered','partially_received') and raise a distinct `'po_not_order |
| SP-065 | P2 | OPEN | inventory-accounting | create() and bulkCreate() silently drop a failed 'initial' movement insert, leaving on-hand and holdings with no ledger row | `apps/web/src/server/services/inventory.ts:2209` | Service layer only. Extract the sized-variants block (inventory.ts:3006-3059: zero levels → zero on-hand with `.select('id')` proof → re-read levels → throw) into one private helper, e.g. `compensateOpeningStockOrThrow(stockedIds, movementErr, label)`, and call it from create() (destructure `{ error |
| SP-066 | P2 | OPEN | auth-sessions | verifyEnrollmentAction accepts enrollment without a password, so a stolen AAL1 cookie can enroll an attacker's TOTP and lock the real owner out | `apps/web/src/server/actions/mfa.ts:91` | Action layer, mfa.ts: change the schema to `password: z.string().min(1, 'Confirm your password').max(128)` (required), make the TS input type `password: string`, and drop the `if (parsed.data.password)` wrapper so the closed-mode rate limit + side-channel check always run — mirroring mfa-recovery.ts |
| SP-067 | P2 | OPEN | async-concurrency-server | integration_deliveries has no claim/lease step: the immediate attempt in dispatchEvent and the drain cron can both deliver the same row, and finalize() overwrites the oth | `apps/web/src/server/services/integration-events.ts:392` | Service layer, apps/web/src/server/services/integration-events.ts. Add `claimDelivery(admin, row, now)`: `update({ next_attempt_at: new Date(now + DELIVERY_TIMEOUT_MS + 5000).toISOString(), attempts: row.attempts + 1 }).eq('id', row.id).eq('status', 'pending').lte('next_attempt_at', now.toISOString( |
| SP-068 | P2 | OPEN | async-concurrency-server | transferStock maps every transfer_stock RPC error to internal_error — forbidden, item_deleted, same_location and quantity_must_be_positive surface as a 500 'An internal e | `apps/web/src/server/services/inventory.ts:4936` | Service layer, inventory.ts transferStock: replace the single throw with an ordered map (specific before general, pattern #28b): `insufficient_stock` → ServiceError('validation_error', "Can't move more than is available in the source location."); `same_location` → 'validation_error' 'Source and dest |
| SP-070 | P2 | OPEN | supabase-query-hygiene | Exception Center reads every positive holding in the org with no pagination — PostgREST's 1000-row cap truncates BEFORE the per-rule cap, so exceptions go missing with `t | `apps/web/src/server/services/exceptions.ts:100` | Service layer: wrap the query in `fetchAllRows((from, to) => q.order('id').range(from, to))` from services/lib/paginate.ts (item_stock_levels has a uuid `id` PK, 0002:145), optionally `{ cap: 20_000 }` with the cap disclosed in `truncatedRules` when hit. Keep PER_RULE_CAP on top. Alternative: push t |
| SP-073 | P2 | OPEN | api-routes | push/register upsert cannot rebind a device token owned by another user under self-only RLS → 500 and stale binding (cross-user push delivery on shared devices) | `apps/web/src/app/api/v1/push/register/route.ts:45` | DB + route + mobile. Migration 0347+: `create function public.register_push_token(p_token text, p_platform text, p_device_id text) returns void security definer set search_path = public` that requires auth.uid() is not null, deletes any push_tokens row with that token owned by another user, then ups |
| SP-074 | P2 | OPEN | react-web-correctness | Server-mode inventory search shows stale server hits after a filter/sort change: the search effect depends only on [q, instantMode] and serverHits is never invalidated wh | `apps/web/src/components/inventory/inventory-table.tsx:1102` | Component layer (inventory-table.tsx:1102): add a memoised `serverFilterKey` string built from the params the fetch consumes (type,status,stock,sort,rack,expected + cat/loc getAll, plus showBookFields) to the effect deps so a settled filter change re-fetches; keep the `!needle` branch as is. Alterna |
| SP-075 | P2 | OPEN | react-web-correctness | Cycle-count row keeps showing an unsaved count (and a computed variance) after the save action fails, so the line looks counted when the DB says uncounted | `apps/web/src/components/cycle-counts/cycle-count-detail.tsx:537` | Component layer only (cycle-count-detail.tsx). Make saveCount/clearLine return `Promise<boolean>`; in CountRow's onBlur do `const ok = await onSave(trimmed); if (!ok) setDraft(line.counted_quantity != null ? String(line.counted_quantity) : '');` and hold a row-local `error` string set from a new `on |
| SP-076 | P2 | OPEN | email-notifications | Fulfillment-family emails (partial, backorder-shipped, return-prompt) advertise RFC 8058 one-click unsubscribe on the public URL but never consult public_email_unsubscrib | `apps/web/src/server/email/return-prompt.ts:160` | Server/email layer, no migration. Move isPublicEmailUnsubscribed out of lib/email/order-requests.ts into a shared server module (e.g. apps/web/src/lib/email/unsubscribe-suppression.ts, fail-OPEN on DB error exactly as today at order-requests.ts:311-323; re-export/import it back in order-requests.ts  |
| SP-078 | P2 | OPEN | mobile-offline-sync | Item photo replace deletes the old image rows and storage objects BEFORE the new row insert; an insert failure leaves the item with no photo and an orphaned object | `apps/mobile/app/item/[id].tsx:1173` | Component (apps/mobile/app/item/[id].tsx uploadAndReplace): reorder to insert-first - insert the new row with is_primary:true and `.select('id').single()` (row proof); on insErr remove the just-uploaded object `path` and alert. Only after the insert succeeds, demote/delete the previous PRIMARY row ( |
| SP-079 | P2 | OPEN | time-money-pagination-forms | Movements date-range filter uses UTC-midnight bounds while rows display browser-local dates, so a one-day filter drops evening movements and includes previous-evening one | `apps/web/src/lib/movements-filters.ts:54` | Page/loader layer + shared helper. (1) lib/movements-filters.ts: give parseFromDateParam/parseToDateParam an optional `timeZone` argument that resolves local midnight in that zone (pure Intl: format the UTC guess with formatToParts in the zone, compute the offset, subtract; two iterations handle DST |
| SP-080 | P2 | OPEN | time-money-pagination-forms | PO receive dialog reports a failed 'Post receipt' only via a transient toast (recurring pattern #20) | `apps/web/src/components/po/po-receive-dialog.tsx:243` | Component only (apps/web/src/components/po/po-receive-dialog.tsx): add `const [serverError, setServerError] = React.useState<string \| null>(null)`; clear it at the top of submit() and in the open-reset effect (:120-130); in the `!res.ok` branch set it alongside the existing toast; render `{serverEr |
| SP-081 | P2 | OPEN | web-mobile-parity | Mobile snapshot delta never removes rows that left scope (archived/deleted items, closed counts, deactivated bundles) from local SQLite | `apps/mobile/src/lib/sync.ts:156` | API route + mobile sync, no migration. snapshot/route.ts: (a) always emit `activeBundleIds: string[]` from a cheap `select id` on active bundles (no `since`), (b) emit `removedItemIds` from a second items query `updated_at >= since and (status <> 'active' or deleted_at is not null)` when `since` is  |
| SP-082 | P2 | OPEN | orders-reservations | Adding a line at picking_complete/packing_slip_generated produces an unstageable order — stageOrder's NULL-quantity_picked guard fires and the only way out is a manager r | `apps/web/src/server/services/order-requests.ts:2726` | Service layer only (apps/web/src/server/services/order-requests.ts stageOrder). Select `picking_completed_at` alongside status/fulfillment_type and narrow the NULL-line count to lines created at or before picking completed: `.is('quantity_picked', null).lte('created_at', picking_completed_at)` (fall |
| SP-083 | P2 | OPEN | inventory-accounting | placementBreakdown() and the list loader label NULL-kind SITE holdings as 'Unplaced' while the row summary counts them as placed | `apps/web/src/server/services/inventory.ts:1846` | Service + loader together (both copies in one PR, pattern #26): coalesce NULL to a distinct 'site' kind, label = `row.locations.name`, rank it 0 alongside racks/crates in both sort functions (inventory.ts:1855, inventory-list.ts:684). No client change is strictly required — inventory-table's awaitin |
| SP-084 | P2 | OPEN | async-concurrency-server | Books import keeps result.created and leaves quantity_on_hand>0 (plus 0199-seeded placements) when the stock_movements batch insert fails — stock with no ledger origin, u | `apps/web/src/server/services/books-import.ts:667` | Service layer, books-import.ts movementErr branch: mirror inventory.ts:2998-3040 verbatim — (a) `item_stock_levels.update({ quantity: 0 }).eq('organization_id').in('item_id', ids)`, (b) `inventory_items.update({ quantity_on_hand: 0, updated_by }).eq('organization_id').in('id', ids).select('id')` and |
| SP-085 | P2 | OPEN | supabase-query-hygiene | Bulk archive stock guard reads holdings for ≤500 items with no pagination — past 1000 holdings the cap silently lets items with stock through | `apps/web/src/server/services/inventory.ts:3981` | Service layer only: add a private `holdingsForItemIds(ids, select)` in inventory.ts that loops `chunkIdsForInFilter(ids, 300)` (existing helper, inventory.ts:7201; 300 because >=395 UUIDs overflow the 16KB response-header limit on Node) and runs `fetchAllRows((from,to) => …in('item_id', chunk).gt('q |
| SP-087 | P2 | OPEN | cycle-counts | post_cycle_count iterates lines without locking them: a count recorded during the post loop is posted at its OLD value while the line row keeps the new one | `supabase/migrations/0343_cycle_count_org_level_location_scope.sql:56` | New migration 0346 (DB layer, pgTAP alongside): a BEFORE UPDATE trigger on cycle_count_lines, `tg_cycle_count_line_require_open`, that does `select status into v_status from public.cycle_counts where id = new.cycle_count_id for key share` and raises `cycle_count_not_open` (P0001) when v_status <> 'i |
| SP-088 | P2 | OPEN | supabase-query-hygiene | Bulk 'Set rack' holdings read is unpaginated — items whose holdings fall past the 1000-row cap are neither moved nor reported as failed | `apps/web/src/server/services/inventory.ts:6294` | Service layer: route this read through the same chunked `fetchAllRows` helper proposed for SP-085 (chunkIdsForInFilter(ids, 300) + `.order('id').range`). Destructure `error` and on failure either throw ServiceError('internal_error') BEFORE the label RPC (move the holdings read ahead of `inventory_se |
| SP-089 | P2 | OPEN | purchasing-receiving | Recurring-PO runner has no claim on the template row — an overlapping cron run (or a manual trigger during a run) creates and can auto-ORDER the same PO twice | `apps/web/src/server/services/recurring-pos.ts:353` | Service layer only (apps/web/src/server/services/recurring-pos.ts runDueTemplates), no migration. Move the advance block ahead of PO creation and make it a claim: compute nextRun as today, then .update({ next_run_at: nextRun, last_run_at: now, updated_by }).eq('organization_id', orgId).eq('id', tpl. |
| SP-092 | P2 | OPEN | async-concurrency-server | Post-response fire-and-forget work (order emails, auto-schedule events, `void audit` rows) is not wrapped in after()/waitUntil, so it can be dropped when the Vercel invoc | `apps/web/src/server/services/order-requests.ts:1943` | Service layer, no migration. Add apps/web/src/server/lib/defer.ts: `export function defer(fn: () => Promise<unknown>) { try { after(() => fn().catch(() => {})) } catch { void fn() } }` — the try/catch matters because `after` throws '`after` was called outside a request scope' (node_modules/next/dist |
| SP-119 | P2 | OPEN | ai-uploads-files | exportInventory tool: warehouseId filters the count probe but is silently dropped from the CSV URL, so the model advertises a count the download does not match | `apps/web/src/lib/ai/tools.ts:1124` | Preferred, api route + tools layer: add an optional `wh` query param to /api/inventory/export.csv (validated as a uuid; `warehouseId: params.get('wh') ?? await getActiveWarehouseFilterFor(ctx)` — InventoryService.list already scopes by getWarehouseAccess so an unreadable warehouse returns nothing) a |
| SP-127 | P2 | OPEN | cycle-counts | Web list() hides multi-warehouse SELECTION counts from warehouse-scoped staff even when assigned to them (mobile shows them) | `apps/web/src/server/services/cycle-counts.ts:238` | Service layer only (apps/web/src/server/services/cycle-counts.ts list(), lines 236-238). Replace `query = query.in('warehouse_id', access.writableIds)` with `query = query.or(`warehouse_id.in.(${access.writableIds.join(',')}),and(warehouse_id.is.null,assigned_to.eq.${this.ctx.userId})`)`. The `and(w |
| SP-129 | P2 | OPEN | auth-sessions | deleteOwnAccountAction's gateMfa() calls withContext(), so an org-less user gets NEXT_REDIRECT swallowed into 'internal_error' and cannot delete their account | `apps/web/src/server/actions/profile.ts:399` | Action + route + page layers, no DB change. (a) profile.ts: replace `await gateMfa()` in deleteOwnAccountAction with a session-scoped MFA resolver modelled on email-change.ts:39-70 resolvePrincipal (supabase.auth.mfa.listFactors -> verified totp => enrolled; if enrolled, getAuthenticatorAssuranceLev |
| SP-130 | P2 | OPEN | orders-reservations | approve() persists internal_notes before the RPC and fail-open (pattern #2): a refused approval still saves the notes | `apps/web/src/server/services/order-requests.ts:1906` | Action + route + service, no migration. (1) actions/order-requests.ts:363 -> `svc.approve(parsed.data.id, parsed.data.internalNotes)` (pass undefined through; explicit null still clears). (2) api/v1/orders/[id]/transition/route.ts:84 -> `svc.approve(id, a.internalNotes)`. (3) order-requests.ts appro |
| SP-131 | P2 | OPEN | api-routes | Public API items list paginates on updated_at with no stable tiebreak → rows skipped/duplicated across pages | `apps/web/src/app/api/public/v1/items/route.ts:34` | API route layer only, one line each: apps/web/src/app/api/public/v1/items/route.ts:34 add `.order('id', { ascending: true })` after the updated_at order; same after `.order('created_at', …)` in public/v1/orders/route.ts:29 and public/v1/purchase-orders/route.ts:29. Add the three route tests. Note in |
| SP-136 | P2 | OPEN | dead-code-duplication | Email validity is decided four different ways: core emailSchema (zod) vs a hand regex copied into customers.ts, rental-create-form.tsx and sage50.ts | `apps/web/src/server/services/customers.ts:354` | packages/core/src/schemas/common.ts: `export const isValidEmail = (s: string) => emailSchema.safeParse(s).success;` and re-export. Replace the regex at rental-create-form.tsx:191 (component), sage50.ts:129 inside sanitizeEmail keeping null-on-junk (lib), customers.ts:354 (service). Optional: have cr |
| SP-029 | P3 | OPEN | tests-quality-gaps | scripts/security-test.sh manifest has drifted: 13+ security-property suites written since PR #92 are absent, so a security regression in them reads as 'a test broke' not  | `scripts/security-test.sh:140` | Test/infra layer only: append to WEB_TESTS in scripts/security-test.sh — src/lib/file-signature.test.ts, src/lib/document-threat-scan.test.ts, src/server/services/attachment-byte-guard.test.ts, src/server/services/capture-byte-guard.test.ts, src/server/services/po-imports.scan-byte-verification.test |
| SP-053 | P3 | OPEN | web-mobile-parity | Mobile free-form Move lists Staging/Unplaced holdings as SOURCE, so Staging -> Unplaced (refused on web) drains the receiving worklist without shelving | `apps/mobile/src/components/move-stock-modal.tsx:279` | Mobile component (OTA) + route guard, no migration. In move-stock-modal.tsx compute destinationKinds from the selected source too: exclude 'unplaced' when `putAwaySourceLocationId` is set OR the currently chosen holding's kind is 'staging'/'unplaced' (re-run the destination query or filter `destinat |
| SP-064 | P3 | OPEN | purchasing-receiving | receive-line route resolves the PO line with .maybeSingle() on item_id — a PO carrying the same item on two lines 500s instead of receiving | `apps/web/src/app/api/v1/po/[id]/receive-line/route.ts:92` | API layer only: replace `.maybeSingle()` with a plain `.select(...).order('created_at', { ascending: true })`, then `const line = lines.find(l => Number(l.quantity_ordered) - Number(l.quantity_received) > 0) ?? lines[0]`; keep `if (lErr)` → 500 for a real read error, `lines.length === 0` → 404 not_o |
| SP-069 | P3 | OPEN | orders-reservations | markInTransit / stageOrder / generatePickSlip status writes have no status predicate; the trigger allows same-status writes, so a duplicate request double-fires emails an | `apps/web/src/server/services/order-requests.ts:2900` | Service layer only: in markInTransit, stageOrder and generatePickSlip append `.eq('status', <expected>)` to the UPDATE, switch `.single()` to `.maybeSingle()`, and `if (!updated) throw new ServiceError('conflict', 'Order status changed — refresh and try again.')` BEFORE audit/notify/dispatch (mirror |
| SP-071 | P3 | OPEN | supabase-query-hygiene | Platform org list computes per-org item/member counts from an unpaginated cross-org SELECT — counts are wrong once the page's orgs hold >1000 items in total | `apps/web/src/server/services/platform/orgs.ts:133` | Service layer (platform/orgs.ts): replace the two tallies with either N parallel `count: 'exact', head: true` per-org head requests (≤500, batched) or one SECURITY DEFINER RPC `platform_org_counts(uuid[]) returns table(organization_id, member_count, item_count)` doing `group by` (new migration 0346+ |
| SP-072 | P3 | OPEN | supabase-query-hygiene | Mobile Categories screen counts items per category with an unbounded org-wide SELECT — counts silently stop at 1000 rows | `apps/mobile/src/screens/categories.tsx:89` | Mobile (component/lib layer, no migration): page the read in 1000-row windows with `.order('id').range(from, to)` until a short page, mirroring apps/web/src/server/services/lib/paginate.ts (which is 'server-only' so cannot be imported by mobile - copy the 12-line loop into src/lib/category-counts.ts |
| SP-086 | P3 | OPEN | time-money-pagination-forms | stagedWorklist source-movement lookup is unbounded and unchunked: PostgREST 1000-row cap silently strips source/age from the OLDEST staged rows - the ones the Stale badge | `apps/web/src/server/services/inventory.ts:6762` | Service layer only (inventory.ts stagedWorklist). Step 1: wrap the holdings query in `fetchAllRows((from, to) => q.order('item_id').order('location_id').range(from, to))` from server/services/lib/paginate.ts. Step 2: `for (const chunk of chunkIdsForInFilter(itemIds, 500))` run the receive_po query p |
| SP-091 | P3 | OPEN | supabase-query-hygiene | Cycle-count AI-scan candidate list reads every line of the count with no pagination — books past line 1000 are 'not in this count' when scanned | `apps/web/src/server/services/cycle-counts.ts:1205` | Service layer: wrap the read in `fetchAllRows((from,to) => …eq('cycle_count_id', id).order('id').range(from,to))` (already imported at line 9). Optionally narrow the embed to `custom_fields->>author` to shrink the payload, and consider a hard upper bound with a clear 422 ('too many titles for one sc |
| SP-093 | P3 | OPEN | migrations-db-security | Live catalog comment on _cycle_count_org_stock_sum still says AR-2 narrowing 'remains blocked' — contradicted by 0331 and SECURITY-INVARIANTS.md (AR-2 RESOLVED) | `supabase/migrations/0327_stock_rpc_integrity_and_nonneg_level_quantity.sql:299` | Fold into NEW migration 0346 (DB metadata): `comment on function public._cycle_count_org_stock_sum(uuid, uuid) is '... (0327). Both prerequisites for AR-2 are met: apply_level_delta is SECURITY DEFINER with its own staff gate since 0331 and item_stock_levels_select is warehouse-scoped since 0331 (se |
| SP-094 | P3 | OPEN | tests-quality-gaps | rentals.test.ts: two tests titled 'releases all stock_reservations for this rental' assert `expect(true).toBe(true)` — the reservation release is unverified | `apps/web/src/server/services/rentals.test.ts:396` | Test layer only (apps/web/src/server/services/rentals.test.ts). Pattern already used in the same file for `deletedRentalIds` and for `audit` mock calls. |
| SP-095 | P3 | OPEN | tests-quality-gaps | Root `pnpm format` / `format:check` cannot run: the glob includes *.sql and no Prettier SQL parser is installed; lint-staged carries the same glob | `package.json:18` | Root package.json: remove `sql` from both prettier globs (`**/*.{ts,tsx,md,json,css}`) and from the lint-staged key. Do NOT add prettier-plugin-sql (it would reformat 345 applied migrations and every pgTAP file). Separately decide whether to commit a `.husky/pre-commit` running `pnpm exec lint-stage |
| SP-096 | P3 | OPEN | tests-quality-gaps | apps/web vitest config uses the deprecated `environmentMatchGlobs` (removed in Vitest 4) — every run prints a DEPRECATED banner | `apps/web/vitest.config.ts:24` | Test infra only, apps/web/vitest.config.ts: replace `environmentMatchGlobs` with `projects: [{ extends: true, test: { name: 'dom', environment: 'happy-dom', include: ['src/components/**/*.test.{ts,tsx}', 'src/app/**/*.test.{ts,tsx}'] } }, { extends: true, test: { name: 'node', environment: 'node', i |
| SP-097 | P3 | OPEN | dead-code-duplication | changePasswordAction inlines its own password side-channel client while verify-password.ts documents itself as the single shared implementation for exactly that flow | `apps/web/src/server/actions/auth.ts:513` | Action layer, apps/web/src/server/actions/auth.ts:513-529 → `const pw = await verifyPasswordSideChannel(user.email, parsed.data.currentPassword); if (!pw.ok) return err('forbidden', 'Current password is incorrect');` and drop the now-unused createSupabaseClient/Database imports if nothing else in th |
| SP-098 | P3 | OPEN | cycle-counts | Post error mappers (web + mobile) do not know the 0342 raise codes — cycle_count_location_out_of_org/out_of_scope surface as raw Postgres text | `apps/web/src/server/services/cycle-counts.ts:158` | Service + mobile lib only (no migration). In mapPostCycleCountError add, BEFORE the generic fallthrough, `cycle_count_location_out_of_scope` → validation_error 'A line was counted at a location outside this count's warehouse. Clear and recount that line, then post again.' and `cycle_count_location_o |
| SP-099 | P3 | OPEN | cycle-counts | sync.ts sendOne 'record_count' branch is unreachable and has drifted from the live drain (duplicate copy) | `apps/mobile/src/lib/sync.ts:357` | Mobile only: delete the `case 'record_count'` block from sendOne in sync.ts (keep the drainQueue `continue` and its comment); reword the cycle-count-sync.ts:297 comment to say it is the ONLY record_count sender. No shared extraction needed since one copy is dead. |
| SP-100 | P3 | OPEN | auth-sessions | consumeMfaRecoveryCodeAction rate limit runs in fail-OPEN mode, unlike every sibling auth limiter | `apps/web/src/server/actions/mfa-recovery.ts:101` | Action layer, mfa-recovery.ts:101: add the fourth argument `'closed'`. Also update the existing tests in mfa-recovery.test.ts to stub `'rpc:increment_rate_limit': { data: { allowed: true, count: 1, reset_at: null }, error: null }` in makeSupabaseStub (or vi.mock '@/lib/rate-limit'), because closed m |
| SP-101 | P3 | OPEN | auth-sessions | requestEmailChange answers 409 for an already-registered target, giving any signed-in user an email-existence oracle | `apps/web/src/server/services/email-change.ts:367` | Service layer, email-change.ts:368-370: replace the throw with the generic-success posture requestPasswordResetAction already uses — do NOT mint or email the target; send the current address a 'that email cannot be used for your account' notice via the existing deliver helper; write an audit row tha |
| SP-102 | P3 | OPEN | orders-reservations | Hand-over notifications and sign-route webhooks label orders by id prefix instead of the SO-number every other event uses | `apps/web/src/server/lib/order-handover-notify.ts:99` | Two files, no DB change. (1) order-handover-notify.ts: add optional `orderNumber?: string \| null` to all three helper arg types; `const orderNo = args.orderNumber ?? '#' + args.orderId.slice(0,8).toUpperCase()` and use it in title + email payload. (2) sign/route.ts: `const soNo = formatOrderNumber( |
| SP-103 | P3 | OPEN | migrations-db-security | next_po_number still has no search_path pin although 0329 claims every remaining first-party function was pinned; no sweep test guards the pin class | `supabase/migrations/0005_phase5_helpers.sql:106` | Fold into NEW migration 0346 (DB): `alter function public.next_po_number(uuid) set search_path = public; revoke execute on function public.next_po_number(uuid) from public, anon; grant execute on function public.next_po_number(uuid) to authenticated, service_role;` with a header note correcting the  |
| SP-104 | P3 | OPEN | email-notifications | Maintenance draft-reminder push copy hard-codes 'sending it to DC4' for every org, after 0337 made the recipients per-org data | `apps/web/src/app/api/cron/maintenance-draft-reminders/route.ts:185` | Cron layer, no migration. Smallest safe: drop the tenant name — body '... Open it in StockPilot to finish sending it.' Optionally resolve per org: batch-select organizations.id,email_routing for the distinct organization_ids in `data`, parse with parseOrgEmailRouting(row.email_routing, 'maintenance_ |
| SP-105 | P3 | OPEN | dead-code-duplication | react-native-nitro-image and react-native-nitro-modules survived the hands-free size-count revert: zero source references, still linked as native pods, and app.config.ts  | `apps/mobile/package.json:66` | Mobile layer only, no server/DB change. (1) Delete lines 66-67 of apps/mobile/package.json and run `pnpm install` to update pnpm-lock.yaml. (2) Rewrite the app.config.ts:26-31 paragraph: 1.4.0's native bump was for react-native-vision-camera + Nitro, both removed in #177 (d8fbee50 / this change), so |
| SP-106 | P3 | OPEN | dead-code-duplication | buildSystemContext copied verbatim into six cron routes and secretsEqual into all nineteen cron/admin routes: any fix to system-actor selection or secret comparison must  | `apps/web/src/app/api/cron/daily-briefing/route.ts:151` | Web layer, mechanical. Create apps/web/src/server/lib/system-context.ts exporting `buildSystemContext(admin, orgId)` (copy the daily-briefing body verbatim, including the `.limit(1)` and `role: 'owner'`) and apps/web/src/lib/auth/cron-secret.ts exporting `secretsEqual` plus `isCronAuthorized(req, .. |
| SP-107 | P3 | OPEN | dead-code-duplication | packages/config/package.json declares main/types/files entries that do not exist | `packages/config/package.json:5` | Config layer only: delete the `main` and `types` keys and the `"tailwind-preset.ts"` files entry from packages/config/package.json, leaving `exports` untouched. |
| SP-108 | P3 | OPEN | cycle-counts | 0282 assign_cycle_count RPC is never called; web assign() bypasses it so assignment_version/claimed_by are not maintained and the 'optimistic-lock token' is read by nothi | `apps/web/src/server/services/cycle-counts.ts:315` | Service layer only, no migration: in assign(), when assignedTo is non-null call `this.ctx.supabase.rpc('assign_cycle_count', { p_count_id: id, p_user_id: assignedTo })` after the existing expectedAssignee pre-read (keep the membership pre-check for the clean message), mapping raises: invalid_assigne |
| SP-109 | P3 | OPEN | auth-sessions | SessionsService.revoke ignores the RPC's row count, so revoking a nonexistent/foreign session id 'succeeds', broadcasts and audits a revocation that never happened | `apps/web/src/server/services/sessions.ts:69` | Service layer, sessions.ts revoke(): `const { data, error } = await ...; if (error) throw new ServiceError('internal_error', error.message); if ((data ?? 0) === 0) throw new ServiceError('not_found', 'That device is already signed out.');` Action layer: in revokeSessionAction's catch, still call `re |
| SP-110 | P3 | OPEN | api-routes | Five export/report/list routes flatten every ServiceError (forbidden, module_disabled, not_found) to HTTP 500 | `apps/web/src/app/api/inventory/export.csv/route.ts:124` | Route layer only: in the five catch blocks replace `{ status: 500 }` with `{ status: serviceErrorStatus(e.code) }` (import from '@/server/services/context'). No service or DB change. |
| SP-112 | P3 | OPEN | migrations-db-security | _cycle_count_location_facts resolves ANY location uuid to its organization_id/warehouse_id/kind/deleted_at for any authenticated user (CONFIRMED) | `supabase/migrations/0342_cycle_count_location_reconciliation.sql:220` | Migration 0346 (DB only, no app change). Keep the 1-arg signature so post_cycle_count (0343) is NOT re-stated: `create or replace function public._cycle_count_location_facts(p_location_id uuid) ... security definer stable set search_path to 'public' as $$ select l.organization_id, l.warehouse_id, l. |
| SP-113 | P3 | OPEN | dead-code-duplication | Dead pre-landing marketing generation: 14 components (2,846 lines) plus two hooks with zero importers, kept by a comment whose 'frame sequence does not survive' claim is  | `apps/web/src/components/marketing/scrolly-landing.tsx:34` | Web layer, owner-gated (memory records it as an open owner decision — confirm the revert window is closed first). Then: delete the 14 components (scrolly-landing, scrolly-pipeline, scrolly-shelf-scan, scrolly-warehouse, hero, hero-frame-parallax, sample-data, feature-grid, stat-band, marquee-strip,  |
| SP-114 | P3 | OPEN | dead-code-duplication | Location classifier, audit-event labels and serial-status labels are hand-mirrored on mobile instead of shared through @stockpilot/core | `apps/mobile/src/lib/po-import-approve.ts:191` | Core + both apps, no DB change. Move apps/web/src/lib/locations/groups.ts (dependency-free) to packages/core/src/locations/groups.ts, export from packages/core/src/index.ts, make apps/web/src/lib/locations/groups.ts a re-export (`export * from '@stockpilot/core'` subset) so the 'use client' Location |
| SP-115 | P3 | OPEN | web-mobile-parity | Mobile stock-api NewRack type still declares RACK XOR CRATE, contradicting core new-location.ts and the body the sheet actually sends | `apps/mobile/src/lib/stock-api.ts:26` | Mobile lib, type-only. In stock-api.ts replace `NewRack` with `import type { NewLocationFields } from '@stockpilot/core'` (mobile already imports core: move-stock-form.ts:57), make `newRack?: NewLocationFields`, delete the XOR paragraph and point the doc at packages/core/src/inventory/new-location.t |
| SP-116 | P3 | OPEN | api-routes | Unvalidated id/uuid params reach PostgREST filters → 22P02 → 500 instead of 400 (public hooks DELETE breaks its idempotent-200 contract) | `apps/web/src/app/api/public/v1/hooks/[id]/route.ts:34` | API route layer only. Add one shared helper apps/web/src/lib/uuid.ts `export const isUuid = (v: string \| null \| undefined): v is string => z.string().uuid().safeParse(v).success` (there is no shared one today — six local UUID_RE copies exist, pattern #26 territory) and apply at the untrusted edge  |
| SP-117 | P3 | OPEN | react-web-correctness | Notification badge is capped at 20 after the first client refetch because the count is taken from a LIMIT 20 query while the label supports 99+ | `apps/web/src/components/dashboard/notification-bell.tsx:91` | Component layer (notification-bell.tsx). Smallest change: request the exact count on the same query — `.select('id, type, title, body, link, created_at', { count: 'exact' })` and `setCount(count ?? data.length)`; PostgREST returns the total matching-row count in the Content-Range header independent  |
| SP-120 | P3 | OPEN | dead-code-duplication | Seven exported server actions have zero callers but remain registered 'use server' endpoints | `apps/web/src/server/actions/inventory.ts:140` | Delete the seven exports (actions layer): inventory.ts archiveItemAction + deleteItemAction, procedures.ts the three category actions, embeddings.ts getMissingEmbeddingsCountAction, integration-endpoints.ts rotateIntegrationEndpointSecretAction. Owner decision flagged, not assumed: InventoryService. |
| SP-121 | P3 | OPEN | dead-code-duplication | Zero-reference exports in packages/core and web lib carry stale duplicates of live values (FROM_EMAIL_DEFAULT on the wrong domain, TRIAL_DAYS, a test seam that deletes a  | `packages/core/src/constants/app.ts:7` | Delete the 13 symbols (core constants/plans/roles/schemas/inventory/customization/sports; web lib/site.ts, services/email-change.ts, lib/rate-limit.ts). For FROM_EMAIL_DEFAULT/APP_DOMAIN/SUPPORT_EMAIL in core: either delete or fix to stockpilotusa.com and make env.ts:62 derive its default from the c |
| SP-122 | P3 | OPEN | web-mobile-parity | Recurring PO enable/disable and delete on mobile skip the audit rows the web service writes | `apps/mobile/app/(drawer)/recurring-pos.tsx:82` | API route + mobile screen. Add apps/web/src/app/api/v1/recurring-pos/[id]/route.ts with PATCH ({enabled}) and DELETE using withApiContext → RecurringPoTemplatesService.setEnabled/remove (the service asserts module + purchase_orders:manage and audits with the ctx passed, so the Bearer path writes exa |
| SP-123 | P3 | OPEN | cycle-counts | start(): assignedTo failure is swallowed — the count is created UNASSIGNED (open to everyone under the 0282 lock) and the caller is told nothing | `apps/web/src/server/services/cycle-counts.ts:978` | Service layer, no migration: (1) before calling start_cycle_count, when input.assignedTo is set, run the same organization_members accepted-member query assign() uses and throw validation_error if absent (no orphan count); (2) replace the bare `catch {}` with `catch (e) { void reportError(e, { tag:  |
| SP-124 | P3 | OPEN | api-routes | v1/po/[id]/receive-line is unreachable from any client and carries a pre-0285 over-receipt guard the outbox would retry forever | `apps/web/src/app/api/v1/po/[id]/receive-line/route.ts:115` | Decide once, code-only. Preferred (keeps the only Bearer PO-receive twin alive for the future outbox wiring the queue was built for): in the API route delete lines 107-124 (both remaining-qty refusals) and compute `remainingAfter` as `remaining - qty` unclamped (negative = over-received, matching th |
| SP-125 | P3 | OPEN | react-web-correctness | Distribute-bundle preview spinner sticks on when the quantity becomes invalid mid-debounce because previewing is never reset on the invalid branch | `apps/web/src/components/bundles/distribute-bundle-modal.tsx:85` | Component layer: add `setPreviewing(false)` to both early-return branches of the effect (the `!open` branch at line 80 and the invalid-input branch at line 85), alongside the existing `setPreview(null)`. |
| SP-126 | P3 | OPEN | mobile-offline-sync | Org switch does not clear persisted effective_permissions / warehouse_scope, so the previous org's nav gating and 'You're viewing X only' banner show under the new org un | `apps/mobile/src/lib/db.ts:277` | Service layer (mobile lib): add `delete from meta where key = 'effective_permissions'; delete from meta where key = 'warehouse_scope';` to clearOrgScopedTables in db.ts (covers both org switch and sign-out), and in use-workspace.ts setActiveOrg call refreshEffectivePermissions() and refreshWarehouse |
| SP-128 | P3 | OPEN | purchasing-receiving | recompute_po_status demotes an import-created 'expected_inbound' PO to 'ordered' after an all-rejected receipt or a full reversal | `supabase/migrations/0183_receipt_lines_allow_signed.sql:53` | New migration 0346+ (never edit 0183): create or replace recompute_po_status so the <= 0 branch restores the pre-receipt open state instead of a constant: set status = case when ordered_at is null and exists (select 1 from public.po_imports i where i.created_from_purchase_order_id = p_po_id) then 'e |
| SP-132 | P3 | OPEN | email-notifications | Digest low-stock section over-pulls only the 150 lowest-quantity items before filtering, silently hiding high-reorder-point items | `apps/web/src/server/services/digest.ts:110` | Service layer, no migration. In getLowStock: replace the single `.limit(150)` select with fetchAllRows((from,to) => supabase.from('inventory_items').select(...).eq('organization_id', orgId).eq('status','active').is('deleted_at', null).or('quantity_on_hand.lte.0,reorder_point.gt.0').order('quantity_o |
| SP-133 | P3 | OPEN | type-escape-hatches | AI getRecentOrders passes an unvalidated model-supplied status through `as any` into the DB filter | `apps/web/src/lib/ai/tools.ts:2328` | Component layer only, apps/web/src/lib/ai/tools.ts. (1) Import `ORDER_STATUS_KEYS` (and the ORDER_STATUS_KEY_SET-backed predicate if exported) from @stockpilot/core customization/order-status, and `type OrderRequestStatus` from @/server/services/order-requests. (2) In getRecentOrdersTool.execute: `c |
| SP-135 | P3 | OPEN | ai-uploads-files | Client-uploaded `-thumb.webp` sidecars are never verified (item-images and maintenance-photos), and ItemImagesService.remove leaves the thumb object behind | `apps/web/src/server/services/item-images.ts:873` | service layer, no migration: in ItemImagesService.record(), when opts.thumbPath is present fetchObjectPrefix(bucket, thumbPath): null head -> store thumb_path null (mobile flows upload master only); head present but sniffImage fails or kind not in {webp,jpeg,png} -> remove both objects and throw inv |
| SP-137 | P3 | OPEN | dead-code-duplication | Four greetingText copies disagree on whether the argument is a first name or a full name, and callers compensate inconsistently | `apps/web/src/lib/email/es/families/fulfillment.ts:160` | Add `firstNameOf(name: string \| null \| undefined): string \| null` to packages/core (so mobile can share it) or apps/web/src/lib/email/es/components.ts, and a single `greetingText(name, {trailing?: boolean})`/`greetingHtml` pair that calls it. Replace the four greetingText/greeting bodies to call  |
| SP-138 | P3 | OPEN | ai-uploads-files | /api/v1/ai/identify-from-photo returns raw model output unvalidated and unscrubbed, and hands client-declared bytes/mime to the vision provider unsniffed | `apps/web/src/app/api/v1/ai/identify-from-photo/route.ts:194` | Route layer only (apps/web/src/app/api/v1/ai/identify-from-photo/route.ts) plus a one-line export. (a) Replace line 54 with the size-counts gate: `const useClaude = resolveAiProvider()==='claude'; if (useClaude ? !env.ANTHROPIC_API_KEY : !env.GEMINI_API_KEY) return 503`. (b) After the size check, `c |
| SP-139 | P3 | OPEN | supabase-query-hygiene | InventoryService.list() placement read for up to 1000 page ids is unpaginated — the 1000-row export loses staged/unplaced/rack columns for items past the holdings cap | `apps/web/src/server/services/inventory.ts:1211` | Service layer: use the same chunked `fetchAllRows` helper as SP-085/SP-088 (chunkIdsForInFilter(ids, 300) + `.order('id').range`) for the 1211 read and surface the error (throw, or log + a `placementUnavailable` flag) instead of `const { data: levels }`. Do not export fetchRowsForItemIdChunks from t |
| SP-140 | P3 | OPEN | email-notifications | Weekly digest cron has no at-most-once key — any second invocation in the same week re-sends every digest | `apps/web/src/app/api/cron/weekly-digest/route.ts:241` | Two layers, migration first. (1) NEW migration 0348+ (0346 and 0347 already exist in the tree): `alter table public.organization_members add column if not exists digest_last_sent_at timestamptz;` with a comment - the stamp must be per (user, org), NOT per user: a user in two orgs legitimately receiv |
| SP-141 | P3 | OPEN | email-notifications | Schedule-reminder and auto-archive recipient queries omit the impersonation_expires_at filter used by every sibling, so a platform admin's 'act as' membership receives or | `apps/web/src/app/api/cron/schedule-reminders/route.ts:164` | Code-only, cron + service layer, two one-line edits: append `.is('impersonation_expires_at', null)` after `.not('accepted_at', 'is', null)` at apps/web/src/app/api/cron/schedule-reminders/route.ts:165 and at apps/web/src/server/services/auto-archive.ts:229, with the same trailing comment the sibling |
| SP-142 | P3 | OPEN | ai-uploads-files | Support-ticket attachmentPath is gated by a bare `startsWith(userId/)` prefix — the exact HI-8 shape — and is later signed with the service-role client | `apps/web/src/server/actions/support-tickets.ts:113` | Action + route layer, no migration required. In lib/storage-path.ts add `export function supportAttachmentPathShape(userId: string): RegExp { return anchored(`${escapeRegExpLiteral(userId)}/${OBJECT_FILENAME_SEGMENT}`); }` (two segments, same as orgLogoPathShape, with a WHY comment naming both mints |
| SP-143 | P3 | OPEN | time-money-pagination-forms | datetime-local 'min' attributes are built from a UTC-sliced ISO string, so the native picker blocks valid same-day times for users west of UTC | `apps/web/src/components/orders/storefront/storefront-cart.tsx:266` | Component layer: build `min` from local components — reuse/extract the pad helper from schedule-event-form.tsx isoToLocalInput into a shared function and call it at storefront-cart.tsx:266 (`new Date(Date.now()+3600e3)`) and rental-create-form.tsx:268 (`new Date()`). No server change; submission con |
| SP-144 | P3 | OPEN | inventory-accounting | resolveAdjustLocation() will route a location-less manual add into an ARCHIVED rack and ignores site (NULL-kind) holdings | `apps/web/src/server/services/inventory.ts:4614` | Service layer only. Replace the two reads in resolveAdjustLocation (inventory.ts:4606-4626) with one: `.from('item_stock_levels').select('location_id, quantity, locations!inner(kind, deleted_at)').eq('item_id', itemId).gt('quantity', 0)` (the same embed shape assertEmptyOrThrow uses), then choose th |

## Open work, grouped

### P1 (9)

- **SP-005** `apps/mobile/app/(drawer)/(tabs)/scan.tsx` — Mobile quick-adjust (scan tab + item screen) calls adjust_stock RPC directly, bypassing every web-side stock:adjust rule
- **SP-007** `apps/mobile/app/po/[id].tsx` — Mobile PO receive calls post_receipt_v2 directly with p_request_hash = the idempotency key, so a retried edit silently returns the OLD receipt as succ
- **SP-008** `apps/web/src/server/actions/profile.ts` — Account deletion reports success on deleteUser failure, but the 'tombstone blocks login' premise is false — no auth funnel reads user_profiles.deleted
- **SP-013** `apps/web/src/server/services/po-imports.ts` — PO-import approve() has no atomic status claim — two overlapping approves create two purchase orders from one import
- **SP-014** `apps/web/src/server/actions/auth.ts` — Password reset via email link cannot complete for any TOTP-enrolled user (GoTrue refuses updateUser at AAL1)
- **SP-018** `apps/mobile/src/components/po-attachments.tsx` — Mobile PO/order attachments write the metadata row directly via PostgREST after a direct storage PUT — the finalize-time sniff + threat scan never run _(needs a migration)_
- **SP-019** `apps/web/src/lib/pdf/po.tsx` — PO expected_at is stored as UTC midnight but rendered in Pacific: supplier-facing PO PDF, PO list (instant mode), mobile ETA and the overdue count all
- **SP-022** `apps/web/src/lib/auth/api-context.ts` — 'Enrolled TOTP forces AAL2' is app-layer only: an AAL1 token reads/writes every org table via direct PostgREST (no RLS policy or membership helper che _(needs a migration)_
- **SP-023** `apps/web/src/server/services/rentals.ts` — Rental markReturned/cancel: status UPDATE is fail-open (no org filter, no row proof) while reservations are released via service role — RLS floor (war

### P2 (66)

- **SP-012** `apps/mobile/app/rentals/new.tsx` — Mobile rental checkout re-implements the web rule divergently: bare rentals header, no lines, no stock reservations, while its comment claims it is th
- **SP-020** `apps/web/src/app/api/orders/sign/route.ts` — Digital signature path never emails internal (member) requesters; backorder emails never reach them on either path
- **SP-024** `apps/web/src/server/services/po-imports.ts` — PO-import approve never checks the `po_imports → approved` UPDATE — a failed stamp leaves a live PO behind an import that can be approved again (dupli
- **SP-025** `apps/web/src/server/services/order-requests.ts` — OrderRequestsService.get().pickSlipStale is always false — line select omits created_at (pattern #5)
- **SP-026** `apps/web/src/app/api/v1/account/delete/route.ts` — Mobile self-deletion audit row is silently dropped: audit() called without ctx in a Bearer route
- **SP-027** `apps/web/src/app/api/v1/size-counts/route.ts` — Raw Postgres/PostgREST/provider error text echoed in API error bodies across ~20 routes (S13 posture violation)
- **SP-028** `supabase/tests/security_invariants.test.sql` — security_invariants sweeps only anon-executable SECURITY DEFINER functions; nothing sweeps authenticated-executable ones, which is how the 0342 helper
- **SP-030** `scripts/dev-local.sh` — Unreferenced scripts/dev-local.sh and dev-prod.sh write THROUGH the .env.local symlink and rewrite the canonical env file outside the repo, dropping e
- **SP-031** `apps/mobile/src/lib/web-path-rewrite.ts` — Four live notification link shapes have no mobile rewrite and dead-end on Home although native twins exist
- **SP-032** `apps/mobile/app/cycle-count/[id].tsx` — Mobile detail loads cycle_count_lines with an unbounded select — silently capped at 1000 rows, so a large count is only partially countable on mobile
- **SP-033** `packages/core/src/constants/permissions.ts` — orders:approve is declared FULLY_GRANTABLE but every order RPC still gates on has_org_role('manager') — granted staff can deny but not approve (patter _(needs a migration)_
- **SP-034** `apps/web/src/components/movements/movements-filter-bar.tsx` — Movements instant-mode search cannot be cleared by backspacing: the debounced q effect early-returns when q equals the constant initial.q, so onChange
- **SP-035** `apps/web/src/app/api/cron/schedule-reminders/route.ts` — Schedule reminders and rental emails print times and the today/tomorrow word in hard-coded America/Los_Angeles although organizations.timezone is a co
- **SP-036** `apps/mobile/app/cycle-count/ai-scan/[id].tsx` — AI-scan confirm double-writes each line: the outbox replay lacks aiScanId and NULLs the ai_scan_id attribution the direct write just set; doubles requ
- **SP-038** `apps/web/src/server/services/attachment-byte-guard.test.ts` — verifyStoredDocumentOrDelete's whole-object re-download and 'unreadable now → fail closed' branches are never exercised — every test stubs totalSize =
- **SP-039** `apps/web/src/server/actions/auth.ts` — completePasswordResetAction has zero tests — its global sign-out + live-session revoke are security properties documented only in comments
- **SP-040** `apps/web/src/lib/ai/tools.ts` — AI aggregation tools request 50,000 / 10,000 rows but PostgREST and MovementsService clamp to 1,000 — totals, top movers and 'dead stock' are silently
- **SP-041** `apps/web/src/server/services/lots.ts` — Lot trace passes an unescaped needle to ilike twice while four private copies of escapeIlike exist in sibling services (pattern #16 via #26)
- **SP-042** `apps/web/src/server/services/audit-log.ts` — Audit log 'Until' date filter excludes the whole selected day: date-only param is passed straight to .lte(created_at)
- **SP-043** `apps/web/src/server/services/order-requests.ts` — Auto-created schedule event 'Needed by ...' details string is formatted in the server's (UTC) local time, then copied into every reminder push/email b
- **SP-044** `packages/core/src/purchasing/recurring.ts` — Recurring PO monthly/quarterly cadence drifts and skips months: setUTCMonth on day 29-31 rolls over, and runDueTemplates chains the roll
- **SP-045** `apps/web/src/server/actions/auth.ts` — changePasswordAction revokes other sessions' refresh tokens but never broadcasts, so other devices keep working for up to an hour after a password cha
- **SP-046** `apps/web/src/lib/email/order-requests.ts` — Order status emails swallow a failed Resend send twice over — no reportError, so an outage in order email delivery is invisible (pattern #28 'a swallo
- **SP-047** `apps/web/src/server/actions/order-requests.ts` — suggestNeededByAction hard-codes a -07:00 / America/Los_Angeles offset instead of the org timezone
- **SP-048** `apps/web/src/components/po/po-form.tsx` — PO size-run add dialog resets typed quantities whenever the page re-renders (same class as the receive dialog): [open, runs] where runs is memoized on
- **SP-049** `apps/web/src/components/realtime/inventory-realtime.tsx` — InventoryRealtime tears down and re-subscribes its Realtime channel on every render because the tables default-parameter array is a fresh identity in 
- **SP-050** `apps/web/src/server/services/order-requests.ts` — OrderRequestsService.cancel — the M7 requester self-cancel guard and error mapping have no test at service or action layer
- **SP-051** `apps/web/src/server/actions/auth.account-disabled.test.ts` — signInAction's MFA step-up decision, closed-mode rate limits, and challengeFactorAction/verifyEnrollmentAction have no behavioural tests — the only si
- **SP-052** `apps/web/src/server/services/rentals.ts` — Rental checkout has no reservation-aware availability guard on any layer: server TODO trusts the picker, and the picker cap uses quantityOnHand instea
- **SP-054** `apps/mobile/app/(drawer)/admin/users.tsx` — Mobile admin role change writes organization_members directly: fail-open 0-row update, no audit row, no security event
- **SP-055** `apps/mobile/app/cycle-count/[id].tsx` — Mobile cycle-count post calls post_cycle_count RPC directly, skipping the module gate, cycle_count.posted audit and cycle_count.completed webhook
- **SP-056** `apps/web/src/server/services/inventory.ts` — stagedWorklist's item_stock_levels query is unpaginated — staged/unplaced holdings past PostgREST's 1000-row cap silently vanish from the put-away wor
- **SP-057** `apps/web/src/components/movements/movements-instant-table.tsx` — Movements page blanks the amount of every order pick and reopen reversal (transfer rows with a real delta and null moved_quantity)
- **SP-058** `apps/web/src/lib/auth/verify-password.ts` — verifyPasswordSideChannel creates a real GoTrue session per check and never signs it out — ghost 'Unknown device' rows in Active Sessions and live ref
- **SP-059** `supabase/migrations/0194_apply_level_delta.sql` — ensure_org_placement_locations / ensure_warehouse_placement_locations let any authenticated user inject Staging/Unplaced location rows into any org or _(needs a migration)_
- **SP-060** `apps/mobile/src/lib/sync.ts` — Mobile offline drain loops (sync.ts drainQueue, CycleCountSyncEngine.drainOutbox) are covered only by source-text string pins; no test executes the ma
- **SP-061** `apps/mobile/src/lib/drain-failure.ts` — Offline outbox retries permanently-rejected 4xx rows forever with no discard path; sync badge sticks on 'failing'
- **SP-062** `apps/web/src/server/services/cycle-counts.ts` — assertSessionAccess on a null-warehouse count reads every line's warehouse_id unbounded — gate silently evaluates a 1000-row subset and runs on every 
- **SP-063** `supabase/migrations/0296_post_receipt_v2_serial_optional.sql` — post_receipt_v2 accepts DRAFT purchase orders, so receiving bypasses the PO approval threshold and flips an unapproved draft straight to received _(needs a migration)_
- **SP-065** `apps/web/src/server/services/inventory.ts` — create() and bulkCreate() silently drop a failed 'initial' movement insert, leaving on-hand and holdings with no ledger row
- **SP-066** `apps/web/src/server/actions/mfa.ts` — verifyEnrollmentAction accepts enrollment without a password, so a stolen AAL1 cookie can enroll an attacker's TOTP and lock the real owner out
- **SP-067** `apps/web/src/server/services/integration-events.ts` — integration_deliveries has no claim/lease step: the immediate attempt in dispatchEvent and the drain cron can both deliver the same row, and finalize(
- **SP-068** `apps/web/src/server/services/inventory.ts` — transferStock maps every transfer_stock RPC error to internal_error — forbidden, item_deleted, same_location and quantity_must_be_positive surface as 
- **SP-070** `apps/web/src/server/services/exceptions.ts` — Exception Center reads every positive holding in the org with no pagination — PostgREST's 1000-row cap truncates BEFORE the per-rule cap, so exception
- **SP-073** `apps/web/src/app/api/v1/push/register/route.ts` — push/register upsert cannot rebind a device token owned by another user under self-only RLS → 500 and stale binding (cross-user push delivery on share _(needs a migration)_
- **SP-074** `apps/web/src/components/inventory/inventory-table.tsx` — Server-mode inventory search shows stale server hits after a filter/sort change: the search effect depends only on [q, instantMode] and serverHits is 
- **SP-075** `apps/web/src/components/cycle-counts/cycle-count-detail.tsx` — Cycle-count row keeps showing an unsaved count (and a computed variance) after the save action fails, so the line looks counted when the DB says uncou
- **SP-076** `apps/web/src/server/email/return-prompt.ts` — Fulfillment-family emails (partial, backorder-shipped, return-prompt) advertise RFC 8058 one-click unsubscribe on the public URL but never consult pub
- **SP-078** `apps/mobile/app/item/[id].tsx` — Item photo replace deletes the old image rows and storage objects BEFORE the new row insert; an insert failure leaves the item with no photo and an or
- **SP-079** `apps/web/src/lib/movements-filters.ts` — Movements date-range filter uses UTC-midnight bounds while rows display browser-local dates, so a one-day filter drops evening movements and includes 
- **SP-080** `apps/web/src/components/po/po-receive-dialog.tsx` — PO receive dialog reports a failed 'Post receipt' only via a transient toast (recurring pattern #20)
- **SP-081** `apps/mobile/src/lib/sync.ts` — Mobile snapshot delta never removes rows that left scope (archived/deleted items, closed counts, deactivated bundles) from local SQLite
- **SP-082** `apps/web/src/server/services/order-requests.ts` — Adding a line at picking_complete/packing_slip_generated produces an unstageable order — stageOrder's NULL-quantity_picked guard fires and the only wa
- **SP-083** `apps/web/src/server/services/inventory.ts` — placementBreakdown() and the list loader label NULL-kind SITE holdings as 'Unplaced' while the row summary counts them as placed
- **SP-084** `apps/web/src/server/services/books-import.ts` — Books import keeps result.created and leaves quantity_on_hand>0 (plus 0199-seeded placements) when the stock_movements batch insert fails — stock with
- **SP-085** `apps/web/src/server/services/inventory.ts` — Bulk archive stock guard reads holdings for ≤500 items with no pagination — past 1000 holdings the cap silently lets items with stock through
- **SP-087** `supabase/migrations/0343_cycle_count_org_level_location_scope.sql` — post_cycle_count iterates lines without locking them: a count recorded during the post loop is posted at its OLD value while the line row keeps the ne _(needs a migration)_
- **SP-088** `apps/web/src/server/services/inventory.ts` — Bulk 'Set rack' holdings read is unpaginated — items whose holdings fall past the 1000-row cap are neither moved nor reported as failed
- **SP-089** `apps/web/src/server/services/recurring-pos.ts` — Recurring-PO runner has no claim on the template row — an overlapping cron run (or a manual trigger during a run) creates and can auto-ORDER the same 
- **SP-092** `apps/web/src/server/services/order-requests.ts` — Post-response fire-and-forget work (order emails, auto-schedule events, `void audit` rows) is not wrapped in after()/waitUntil, so it can be dropped w
- **SP-119** `apps/web/src/lib/ai/tools.ts` — exportInventory tool: warehouseId filters the count probe but is silently dropped from the CSV URL, so the model advertises a count the download does 
- **SP-127** `apps/web/src/server/services/cycle-counts.ts` — Web list() hides multi-warehouse SELECTION counts from warehouse-scoped staff even when assigned to them (mobile shows them)
- **SP-129** `apps/web/src/server/actions/profile.ts` — deleteOwnAccountAction's gateMfa() calls withContext(), so an org-less user gets NEXT_REDIRECT swallowed into 'internal_error' and cannot delete their
- **SP-130** `apps/web/src/server/services/order-requests.ts` — approve() persists internal_notes before the RPC and fail-open (pattern #2): a refused approval still saves the notes
- **SP-131** `apps/web/src/app/api/public/v1/items/route.ts` — Public API items list paginates on updated_at with no stable tiebreak → rows skipped/duplicated across pages
- **SP-136** `apps/web/src/server/services/customers.ts` — Email validity is decided four different ways: core emailSchema (zod) vs a hand regex copied into customers.ts, rental-create-form.tsx and sage50.ts

### P3 (51)

- **SP-029** `scripts/security-test.sh` — scripts/security-test.sh manifest has drifted: 13+ security-property suites written since PR #92 are absent, so a security regression in them reads as
- **SP-053** `apps/mobile/src/components/move-stock-modal.tsx` — Mobile free-form Move lists Staging/Unplaced holdings as SOURCE, so Staging -> Unplaced (refused on web) drains the receiving worklist without shelvin
- **SP-064** `apps/web/src/app/api/v1/po/[id]/receive-line/route.ts` — receive-line route resolves the PO line with .maybeSingle() on item_id — a PO carrying the same item on two lines 500s instead of receiving
- **SP-069** `apps/web/src/server/services/order-requests.ts` — markInTransit / stageOrder / generatePickSlip status writes have no status predicate; the trigger allows same-status writes, so a duplicate request do
- **SP-071** `apps/web/src/server/services/platform/orgs.ts` — Platform org list computes per-org item/member counts from an unpaginated cross-org SELECT — counts are wrong once the page's orgs hold >1000 items in
- **SP-072** `apps/mobile/src/screens/categories.tsx` — Mobile Categories screen counts items per category with an unbounded org-wide SELECT — counts silently stop at 1000 rows
- **SP-086** `apps/web/src/server/services/inventory.ts` — stagedWorklist source-movement lookup is unbounded and unchunked: PostgREST 1000-row cap silently strips source/age from the OLDEST staged rows - the 
- **SP-091** `apps/web/src/server/services/cycle-counts.ts` — Cycle-count AI-scan candidate list reads every line of the count with no pagination — books past line 1000 are 'not in this count' when scanned
- **SP-093** `supabase/migrations/0327_stock_rpc_integrity_and_nonneg_level_quantity.sql` — Live catalog comment on _cycle_count_org_stock_sum still says AR-2 narrowing 'remains blocked' — contradicted by 0331 and SECURITY-INVARIANTS.md (AR-2 _(needs a migration)_
- **SP-094** `apps/web/src/server/services/rentals.test.ts` — rentals.test.ts: two tests titled 'releases all stock_reservations for this rental' assert `expect(true).toBe(true)` — the reservation release is unve
- **SP-095** `package.json` — Root `pnpm format` / `format:check` cannot run: the glob includes *.sql and no Prettier SQL parser is installed; lint-staged carries the same glob
- **SP-096** `apps/web/vitest.config.ts` — apps/web vitest config uses the deprecated `environmentMatchGlobs` (removed in Vitest 4) — every run prints a DEPRECATED banner
- **SP-097** `apps/web/src/server/actions/auth.ts` — changePasswordAction inlines its own password side-channel client while verify-password.ts documents itself as the single shared implementation for ex
- **SP-098** `apps/web/src/server/services/cycle-counts.ts` — Post error mappers (web + mobile) do not know the 0342 raise codes — cycle_count_location_out_of_org/out_of_scope surface as raw Postgres text
- **SP-099** `apps/mobile/src/lib/sync.ts` — sync.ts sendOne 'record_count' branch is unreachable and has drifted from the live drain (duplicate copy)
- **SP-100** `apps/web/src/server/actions/mfa-recovery.ts` — consumeMfaRecoveryCodeAction rate limit runs in fail-OPEN mode, unlike every sibling auth limiter
- **SP-101** `apps/web/src/server/services/email-change.ts` — requestEmailChange answers 409 for an already-registered target, giving any signed-in user an email-existence oracle
- **SP-102** `apps/web/src/server/lib/order-handover-notify.ts` — Hand-over notifications and sign-route webhooks label orders by id prefix instead of the SO-number every other event uses
- **SP-103** `supabase/migrations/0005_phase5_helpers.sql` — next_po_number still has no search_path pin although 0329 claims every remaining first-party function was pinned; no sweep test guards the pin class _(needs a migration)_
- **SP-104** `apps/web/src/app/api/cron/maintenance-draft-reminders/route.ts` — Maintenance draft-reminder push copy hard-codes 'sending it to DC4' for every org, after 0337 made the recipients per-org data
- **SP-105** `apps/mobile/package.json` — react-native-nitro-image and react-native-nitro-modules survived the hands-free size-count revert: zero source references, still linked as native pods
- **SP-106** `apps/web/src/app/api/cron/daily-briefing/route.ts` — buildSystemContext copied verbatim into six cron routes and secretsEqual into all nineteen cron/admin routes: any fix to system-actor selection or sec
- **SP-107** `packages/config/package.json` — packages/config/package.json declares main/types/files entries that do not exist
- **SP-108** `apps/web/src/server/services/cycle-counts.ts` — 0282 assign_cycle_count RPC is never called; web assign() bypasses it so assignment_version/claimed_by are not maintained and the 'optimistic-lock tok
- **SP-109** `apps/web/src/server/services/sessions.ts` — SessionsService.revoke ignores the RPC's row count, so revoking a nonexistent/foreign session id 'succeeds', broadcasts and audits a revocation that n
- **SP-110** `apps/web/src/app/api/inventory/export.csv/route.ts` — Five export/report/list routes flatten every ServiceError (forbidden, module_disabled, not_found) to HTTP 500
- **SP-112** `supabase/migrations/0342_cycle_count_location_reconciliation.sql` — _cycle_count_location_facts resolves ANY location uuid to its organization_id/warehouse_id/kind/deleted_at for any authenticated user (CONFIRMED) _(needs a migration)_
- **SP-113** `apps/web/src/components/marketing/scrolly-landing.tsx` — Dead pre-landing marketing generation: 14 components (2,846 lines) plus two hooks with zero importers, kept by a comment whose 'frame sequence does no
- **SP-114** `apps/mobile/src/lib/po-import-approve.ts` — Location classifier, audit-event labels and serial-status labels are hand-mirrored on mobile instead of shared through @stockpilot/core
- **SP-115** `apps/mobile/src/lib/stock-api.ts` — Mobile stock-api NewRack type still declares RACK XOR CRATE, contradicting core new-location.ts and the body the sheet actually sends
- **SP-116** `apps/web/src/app/api/public/v1/hooks/[id]/route.ts` — Unvalidated id/uuid params reach PostgREST filters → 22P02 → 500 instead of 400 (public hooks DELETE breaks its idempotent-200 contract)
- **SP-117** `apps/web/src/components/dashboard/notification-bell.tsx` — Notification badge is capped at 20 after the first client refetch because the count is taken from a LIMIT 20 query while the label supports 99+
- **SP-120** `apps/web/src/server/actions/inventory.ts` — Seven exported server actions have zero callers but remain registered 'use server' endpoints
- **SP-121** `packages/core/src/constants/app.ts` — Zero-reference exports in packages/core and web lib carry stale duplicates of live values (FROM_EMAIL_DEFAULT on the wrong domain, TRIAL_DAYS, a test 
- **SP-122** `apps/mobile/app/(drawer)/recurring-pos.tsx` — Recurring PO enable/disable and delete on mobile skip the audit rows the web service writes
- **SP-123** `apps/web/src/server/services/cycle-counts.ts` — start(): assignedTo failure is swallowed — the count is created UNASSIGNED (open to everyone under the 0282 lock) and the caller is told nothing
- **SP-124** `apps/web/src/app/api/v1/po/[id]/receive-line/route.ts` — v1/po/[id]/receive-line is unreachable from any client and carries a pre-0285 over-receipt guard the outbox would retry forever
- **SP-125** `apps/web/src/components/bundles/distribute-bundle-modal.tsx` — Distribute-bundle preview spinner sticks on when the quantity becomes invalid mid-debounce because previewing is never reset on the invalid branch
- **SP-126** `apps/mobile/src/lib/db.ts` — Org switch does not clear persisted effective_permissions / warehouse_scope, so the previous org's nav gating and 'You're viewing X only' banner show 
- **SP-128** `supabase/migrations/0183_receipt_lines_allow_signed.sql` — recompute_po_status demotes an import-created 'expected_inbound' PO to 'ordered' after an all-rejected receipt or a full reversal _(needs a migration)_
- **SP-132** `apps/web/src/server/services/digest.ts` — Digest low-stock section over-pulls only the 150 lowest-quantity items before filtering, silently hiding high-reorder-point items
- **SP-133** `apps/web/src/lib/ai/tools.ts` — AI getRecentOrders passes an unvalidated model-supplied status through `as any` into the DB filter
- **SP-135** `apps/web/src/server/services/item-images.ts` — Client-uploaded `-thumb.webp` sidecars are never verified (item-images and maintenance-photos), and ItemImagesService.remove leaves the thumb object b
- **SP-137** `apps/web/src/lib/email/es/families/fulfillment.ts` — Four greetingText copies disagree on whether the argument is a first name or a full name, and callers compensate inconsistently
- **SP-138** `apps/web/src/app/api/v1/ai/identify-from-photo/route.ts` — /api/v1/ai/identify-from-photo returns raw model output unvalidated and unscrubbed, and hands client-declared bytes/mime to the vision provider unsnif
- **SP-139** `apps/web/src/server/services/inventory.ts` — InventoryService.list() placement read for up to 1000 page ids is unpaginated — the 1000-row export loses staged/unplaced/rack columns for items past 
- **SP-140** `apps/web/src/app/api/cron/weekly-digest/route.ts` — Weekly digest cron has no at-most-once key — any second invocation in the same week re-sends every digest _(needs a migration)_
- **SP-141** `apps/web/src/app/api/cron/schedule-reminders/route.ts` — Schedule-reminder and auto-archive recipient queries omit the impersonation_expires_at filter used by every sibling, so a platform admin's 'act as' me
- **SP-142** `apps/web/src/server/actions/support-tickets.ts` — Support-ticket attachmentPath is gated by a bare `startsWith(userId/)` prefix — the exact HI-8 shape — and is later signed with the service-role clien
- **SP-143** `apps/web/src/components/orders/storefront/storefront-cart.tsx` — datetime-local 'min' attributes are built from a UTC-sliced ISO string, so the native picker blocks valid same-day times for users west of UTC
- **SP-144** `apps/web/src/server/services/inventory.ts` — resolveAdjustLocation() will route a location-less manual add into an ARCHIVED rack and ignores site (NULL-kind) holdings

